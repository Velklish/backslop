# BS-43 · Результат

**Закрыта 2026-09-07.** Выполнена. Перенос задачи с переписью ссылок вынесен в `relocateTask` (`lib/tasks.js`) — она двигает файл, пересчитывает исходящие ссылки от старого каталога к новому, переписывает входящие по всему markdown репозитория и возвращает список изменившихся путей; `--dry-run` считает тот же список, ничего не записывая. `lib/mv.js` и `lib/archive.js` зовут её вместо двух копий цикла, а `archive` подхватил `findFlatTask` запасным поиском — файл, лежащий плоско в `docs/backlog/`, теперь архивируется так же, как задача из каталога статуса, тогда как раньше `archive N` на нём отказывал там, где `mv N` тот же файл находил. Третий случай одной истории (`BS-4.1`, `BS-9` чинили `mv` вслед за `archive`) закрыт в обратную сторону.

**Проверки.** Новый тест архива плоского файла — исходящая ссылка на `../reference/`, входящая из `ROADMAP.md`, обе пересчитаны, `lint` после архивации зелёный. Целевые тесты `archive`, `commands`, `lint`, `tasks` — 70 из 70, код 0; `node bin/backslop.js lint` — 0 ошибок; полный `npm test` в основном дереве — 138 из 138, код 0. Мутационная проба после коммита: удаление `findFlatTask` из `archive` — 2/3, краснеет ровно новый тест.

**Доки тем же ходом.** [02-cli](../../reference/02-cli.md) — строка `archive` теперь называет перепись входящих ссылок и плоский файл; `CHANGELOG.md` — запись в «Не выпущено».

**Ревью.** Изолированное ревью не состоялось: две сессии reviewer'а на Codex встали на первом вызове `promptobus_mailbox`. Дифф разобран оркестратором построчно — проверено, что `relocateTask` перенесена дословно, `moveFile` внутри неё по-прежнему создаёт каталог архива, а `mv` берёт признаки `outgoingChanged` и `relinked` из её возврата.
