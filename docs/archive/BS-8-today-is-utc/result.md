# BS-8 · Результат

**Закрыта 2026-09-05.** Выполнена.

`today()` в `lib/util.js` собирает дату из `getFullYear/getMonth/getDate` — календарная дата машины по её часовому поясу, а не `toISOString().slice(0, 10)` по UTC. Затронуты все даты команд: «Создана», «Взята», «Отложена», «Закрыта». Проверено: `test/commands.test.mjs` — `new` под `TZ=Etc/GMT-14` и `TZ=Etc/GMT+12` пишет разные «Создана» (пояса разнесены на 26 часов и никогда не в одном дне); мутационная проба с возвратом UTC красит тест.

**Доки тем же ходом.** Справочник 02 (абзац про даты команд), CHANGELOG.
