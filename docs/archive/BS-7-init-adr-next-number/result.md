# BS-7 · Результат

**Закрыта 2026-09-05.** Выполнена.

`init` считает номер ADR процесса по `scanAdrs`: пустой `adr/` — 001, иначе следующий свободный; шаблоны ADR и строки таблицы README берут `{{adrNumber}}`; уже лежащий `adr-NNN-process.md` повторный `init` не дублирует. Проверено: `init` над двумя чужими ADR создаёт `adr-003-process.md` и называет его в напоминании, повтор не создаёт четвёртого; в пустом проекте строка таблицы и файл называют один номер (`test/init.test.mjs`).
