# BS-45 · Результат

**Закрыта 2026-09-09.** Выполнена. Тесты CLI ассертят текст отказа и перечень путей вместо суррогатов: `/занят дважды/` у `new --parent`, `/нет ни в одном каталоге статуса/` у `archive 42`, `deepEqual` на перечень шести путей `--dry-run` — число выводится из длины перечня, списки сравниваются отсортированными, потому что порядок обхода `repoMarkdown` идёт из readdir. Тестовая база больше не держит `lint` с постоянным предупреждением: `makeProject` по умолчанию ставит штамп `version: TOOL_VERSION`, раскладка без штампа — `makeProject({ stamp: false })` для трёх тестов legacy-проекта (`test/lint.test.mjs`, `test/upgrade.test.mjs`). Инвариант «зелёный lint молчит и в stderr» закреплён в тесте-владельце `test/lint.test.mjs`, одна попутная копия в `test/review.test.mjs`. Оснастка `cli()` дописывает дочернему процессу `NODE_OPTIONS=--no-warnings`: под harness'ом с `FORCE_COLOR=3` Node печатал в stderr «Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set», и ассерт на пустой stderr краснел по причине, к продукту не относящейся; сообщения самого backslop идут через `console.warn` и не гасятся.

**Проверки.** Worker: `npm test` — 139 из 139, код 0; `node bin/backslop.js lint` — 0 ошибок, код 0. Мутационные пробы после коммита: `CliError` в `lib/archive.js:22` заменён на `TypeError` — 1 красный на новом ассерте, до правки та же мутация была зелёной; печать перечня `--dry-run` снята — 1 красный на `deepEqual`; `findTask` в `lib/new.js:39` бросает `TypeError` — 1 красный ровно на новом ассерте; штамп снят по умолчанию — 3 красных «зелёный lint молчит и в stderr»; `--no-warnings` снят под `FORCE_COLOR=3` — 2 красных с текстом предупреждения Node в `actual`. На `main` после приёмки: `npm test` — 138 из 138, код 0; `lint` — код 0.

**Доки тем же ходом.** Не потребовалось: правка не меняет поведения продукта, только проверки; по «Не входит» карточки CHANGELOG и reference не трогаются.

**Ревью.** Изолированное ревью (reviewer Claude, база `1b1f226`): три замечания — лишний `cli` в штампе фикстуры, снятие одного `FORCE_COLOR` вместо класса предупреждений Node, ассерт-инвариант не у теста-владельца. Все закрыты коммитом review fix, закрытие сверено оркестратором по диффу.
