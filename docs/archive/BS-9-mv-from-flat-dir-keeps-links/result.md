# BS-9 · Результат

**Закрыта 2026-09-05.** Выполнена, по первой ветке развилки: отдельной команды миграции нет.

`mv` пересчитывает исходящие ссылки переехавшего файла от старого каталога к новому тем же `rewriteMovedLinks`, что и `archive`, и подбирает файл задачи, лежащий прямо в `docs/backlog/` (`findFlatTask` в `lib/tasks.js`): плоский бэклог чужого трекера раскладывается по статусам командой, а не `git mv` руками. Побочно закрыт соседний дефект: при переезде между каталогами статусов ссылка на соседнюю задачу прежнего каталога (`BS-1-a.md`) получала бы обрыв, теперь становится `../queue/BS-1-a.md`. Проверено: `test/commands.test.mjs` — плоский файл со ссылками `../reference/…`, `queue/…`, `../archive/…` после `mv 5 queue` лежит в `queue/` с `../../reference/…`, `BS-1-a.md`, `../../archive/…`, входящая ссылка из ROADMAP поправлена, `lint` зелёный до и после второго переезда в `active/`; мутационная проба без пересчёта красит тест.

**Доки тем же ходом.** Справочник 02 (строка `mv`) и 03 (чем чинится гейт 3), CHANGELOG.
