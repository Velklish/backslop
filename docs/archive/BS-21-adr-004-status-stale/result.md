# BS-21 · Результат

**Закрыта 2026-09-09.** Выполнена. Шапка ADR-004 — `Superseded in part by ADR-007 (источник релизов при npm-форме)`, той же формой, что у пары ADR-006/ADR-008; строка таблицы `docs/README.md` — та же пометка; пункт Consequences ADR-004 про вывод источника из формы оставлен дословно с пометкой «— заменено ADR-007»: принцип `docs/README.md` («решение не правится, а заменяется с пометкой») сильнее строки карточки, просившей пункт переписать; в Consequences ADR-007 — зеркальная строка про частичную замену ADR-004; комментарий в `lib/config.js` называет ADR-007 одной строкой.

**Проверки.** Актуализация: все улики карточки подтвердились дословно, строки не уехали. Worker: `npm test` — 169 из 169, код 0; `lint` — 0 ошибок, включая новую ссылку adr-004 → adr-007. `grep` по `docs/`, `lib/`, `README.md`, `README.ru.md`: утверждения про вывод источника из формы больше нигде нет. Мутационной пробы нет — задача бумажная. На `main` после приёмки: `lint` — код 0, `node --test test/config.test.mjs` — код 0.

**Доки тем же ходом.** Сама правка — документация; CHANGELOG не трогается.

**Ревью.** Дифф без кода — принят оркестратором; два замечания (исходный текст пункта с пометкой вместо переписи; зеркальная строка в ADR-007) закрыты коммитом review fix.
