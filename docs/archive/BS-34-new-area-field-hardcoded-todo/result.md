# BS-34 · Результат

**Закрыта 2026-09-09.** Выполнена. `new` кладёт в «Область» рабочую ссылку с текстом-заглушкой TODO и целью `<путь>/README.md` справочника (EN — «section»), путь — `toPosix(path.relative(dirs.statusDir[status], dirs.reference))`, а не `path.posix.relative`, как буквально просила карточка: входы здесь нативные абсолютные, и на Windows posix-вариант по ним врёт. Заглушка в тексте ссылки — стык с гейтом BS-25: ссылка рабочая, поле числится незаполненным до выбора раздела. Без `docs/reference/README.md` (по замечанию ревью) остаётся прежняя текстовая заглушка — иначе гейт 1 краснел бы в проекте без справочника; названо в reference/01 и 02. Тест — в `test/commands.test.mjs` (файла `new.test.mjs` нет, все тесты `new` живут там), ожидаемое берётся из `loadProject(root).dirs`.

**Проверки.** Worker: `npm test` — 145 из 145 (146 после merge main), затем 158 из 158, код 0; `lint` — 0 ошибок. Мутационные пробы после коммитов: замена расчёта на литерал `'../../reference/README.md'` — тест не краснеет, и это свойство раскладки (все каталоги статусов — братья `reference/` под одним `docs`, `path.relative` всегда даёт `../..`), а не дыра теста: тест сверяет значение с контрактом `layout()`; возврат к статичному тексту — красный; `existsSync` снят — красный «new: без docs/reference/README.md». На `main` после приёмки: `npm test` — 168 из 168, код 0; `lint` — код 0.

**Доки тем же ходом.** [01-layout](../../reference/01-layout.md), [02-cli](../../reference/02-cli.md), `CHANGELOG.md`.

**Ревью.** Изолированное ревью (reviewer Claude, база `7f0c52d`): одно замечание — ссылка роняет гейт 1 в проекте без справочника; закрыто заглушкой по `existsSync`, сверено оркестратором по диффу.
