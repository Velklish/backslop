# BS-61 · Результат

**Закрыта 2026-09-18, выполнена.** Пятый каталог статуса `minor/` в `STATUSES`; поле «Цена» с разбором `parseCost` (уровни `critical`/`major`/`minor`, пометка гипотезы); `new <slug> --parent N --minor [--cost <уровень>] [--hypothesis]` заводит запись из шаблона `minor.md` (ru и en) с пустой «Областью», полями «Родитель» и «Цена» и разделом «Улика»; пять отказов флагов. `mv N minor` дописывает «Цена: minor» и стирает заглушку области. `status` печатает секцию `Minor` по областям, `status --json` отдаёт `minor` с `area` и `cost`. `lint`: пустая или незаполненная «Область» в `minor/` — предупреждение (гейт заглушек строку «Области» в `minor/` пропускает), отсутствующая или неразбираемая «Цена» и `major`/`critical` без пометки гипотезы — ошибка. Первая миграция `migrate` создаёт `docs/backlog/minor/.gitkeep`; проект без штампа считается старше любой миграции. Справка `help` ru/en описывает флаги и статус.

**Проверки.** `node bin/backslop.js gates` на adef059: код 0, гейтов 2, зелёных 2; `npm test` 281/281 (на базе b0a950c — 266). Красные пробы в `test/lint.test.mjs`: нет каталога `minor/`, нет «Цены», «Цена» не разбирается, `major` без гипотезы, «Цена» повторяется, заглушка вне «Области» в `minor/`; зелёный тест на предупреждения (три случая, код 0). Мутационная проба на коммите 6aade63: инверсия проверки `parsed.level !== 'minor'` в `lib/lint.js` — 2 теста покраснели (80/82), восстановлено, 82/82. Ревью изолированным reviewer'ом (claude-opus-5 xhigh, база b0a950c): три круга, находок 9 → 1 → 0; по этой карточке закрыты справка `help`, заглушка «Области» в `minor/`, проба границы исключения; хвост про `since > TOOL_VERSION` в `MIGRATIONS` — запись [BS-61.1](../BS-61.1-migration-since-above-tool-version/task.md) в `minor/`.

**Доки тем же ходом.** `docs/reference/01-layout.md` (каталог, поля «Родитель» и «Цена», раздел «Файл minor»), `02-cli.md` (строки `new`, `mv`, `status`, `migrate`, `status --json`, контракт), `03-lint.md` (гейт 4, предупреждения), `docs/GLOSSARY.md`, `CHANGELOG.md`.
