# BS-62 · Результат

**Закрыта 2026-09-18, выполнена.** `archive N.k --into M` переносит запись из `minor/` в `docs/archive/<M>-<slug>/minor/` через `relocateTask` с переписью ссылок обеих сторон, без своего `result.md`; вывод называет `result.md` пачки местом исхода. Отказы: запись не в `minor/`, пачки нет, пачка сама minor-запись, пачка ещё не закрыта, `--range` вместе с `--into`. Отклонение от плана: незакрытая пачка — отказ, а не предупреждение: smoke-прогон показал, что каталог незакрытой пачки в архиве читался вторым файлом её номера и ломал `archive M` и `lint`. `scanTasks` видит записи `archive/<dir>/minor/*.md` как закрытые с полем `into`, `foreignTaskIds` считает их номера по диску worktree и по `git ls-tree`; `status` в счёте архива их не считает. Гейт архива пускает в `minor/` пачки только файлы записей; справка `help` ru/en описывает форму команды.

**Проверки.** `node bin/backslop.js gates` на adef059: код 0, гейтов 2, зелёных 2; `npm test` 281/281. Тест `test/commands.test.mjs`: перенос с `git mv`, перепись входящей ссылки, отсутствие `result.md`, пять отказов, следующий `N.k` после закрытой записи, счёт архива в JSON. Красные пробы `lint`: чужой файл и каталог в `minor/` пачки, чужой заголовок у записи; зелёный тест на упоминание закрытой записи. Мутационная проба на коммите a50a76a: инверсия проверки статуса в `archiveInto` и условия в гейте архива — 2 теста покраснели (119/121), восстановлено. Ревью: три круга, по этой карточке находок нет; reviewer сверил все пять отказов и обе стороны переписи ссылок.

**Доки тем же ходом.** `docs/reference/01-layout.md` (раздел «Архив»), `02-cli.md` (строка `archive`, отказы, контракт), `03-lint.md` (гейты 5 и 6), `CHANGELOG.md`.
