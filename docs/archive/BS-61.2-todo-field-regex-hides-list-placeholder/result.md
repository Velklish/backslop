# BS-61.2 · Результат

**Закрыта 2026-09-22.** Выполнена частично — по решению владельца, принятому в ходе ревью. Разбор строки починен: `isTodoPlaceholder` снимает маркер списка до разбора поля, и открывающая скобка с TODO больше не читается именем поля, поэтому заглушка с подсказкой в `queue/` красит гейт наравне с голой.

**Миграция, заведённая было в этом же заходе, снята целиком.** Она переписывала заглушку с подсказкой в голую по всему `docs/backlog/**` проекта — и изолированное ревью показало, что смысла в этом нет: после починки гейт красит обе формы одинаково, то есть перепись превращает красное в красное и не меняет ни одного вердикта нигде. В `triage/`, куда гейт после BS-64 не смотрит вовсе, она только удаляла авторский текст, прямо противореча строке, добавленной этой же веткой: заготовки в `triage/` остаются как есть. Под регулярку не попадало ничто из того, что пишет сам инструмент, — бить она могла только по тексту, написанному человеком (235 таких строк в бэклоге aivals).

**Проверки.** `node bin/backslop.js gates --require-clean` на `aab1bad` — код 0, «гейтов 2, зелёных 2», tests 284 / pass 284 / fail 0. Мутационная проба: `lib/lint.js:353` — код 1, 89/85/0, покраснело четыре теста. Снятие миграции полное и проверяется без чтения кода: `git diff --quiet 1330e9e..HEAD -- lib/migrate.js` и то же для `test/upgrade.test.mjs` дают код 0, то есть оба файла идентичны базе байт в байт.

**Доки тем же ходом.** `CHANGELOG.md` — запись про гейт (фраза «долг был не виден, это не регресс» осталась при ней, потому что без миграции верна именно она); `docs/reference/02-cli.md` — строка `migrate` вернулась к базовой формулировке.
