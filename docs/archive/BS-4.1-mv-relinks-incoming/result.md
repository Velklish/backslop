# BS-4.1 · Результат

**Закрыта 2026-09-03.** Выполнена. `mv` после переноса переписывает входящие ссылки на файл задачи по всему markdown репозитория тем же обходом и функцией, что `archive`; команда называет поправленные файлы. Найдено при взятии BS-4 в работу: ссылка из ROADMAP на очередь осталась битой.

**Проверки.** Тест в `test/commands.test.mjs`: ссылки из ROADMAP, соседней задачи и корневого README после `mv` ведут в новый каталог, `lint` зелёный. Мутационная проба (обход ограничен `docs/`, откат после прогона): красная 1 из 7 — на ссылке из корневого README.

**Доки тем же ходом.** `docs/reference/02-cli.md` (строка `mv`), CHANGELOG.
