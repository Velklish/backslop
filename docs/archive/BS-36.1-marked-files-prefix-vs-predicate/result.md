# BS-36.1 · Результат

**Закрыта 2026-09-09.** Выполнена. Из двух определений верным признан предикат: `markedFiles` (`lib/adapters.js`) снял фильтр «первый сегмент `backslop-*`» и идёт по маркеру, как `isOwnedAdapterFile`. Это совпадает с ADR-006 и справочником («владение — маркер под корнем harness плюс legacy-набор»); сужение предиката потребовало бы нового ADR и правки `test/adapter-ownership.test.mjs`.

**Проверки.** Тест `init: файл с маркером вне backslop-* под корнем harness — owned и для предиката, и для снятия` (`test/init.test.mjs`): `.claude/skills/other/note.md` с маркером — owned по предикату и снят `init`, сосед без маркера остаётся и в предупреждение не попадает. Мутационная проба после коммита: вернуть фильтр — 1 из 1 красный. Гейты в worktree и на `main` — как у BS-19.1 (одна приёмка).

**Доки тем же ходом.** [01-layout](../../reference/01-layout.md) — маркер под корнем признаётся по любому пути; снятие такого файла молчаливое, закоммиченный виден в `git status`, незакоммиченный уходит без следа; `CHANGELOG.md`.

**Ревью.** Общее с BS-19.1 (пять кругов): minor — обоснование в справочнике было верно только для tracked-файла, фраза сужена. Закрыто.
