# BS-64 · Результат

**Закрыта 2026-09-22.** Выполнена. Гейт заглушек больше не смотрит в `triage/` вовсе — ни «Область», ни заглушки в секциях там не красят `lint` (`lib/lint.js`, `lintBacklogTodos` пропускает `dirs.statusDir.triage`). Шаблон `new` не менялся: карточка в `triage/` остаётся полной, а README бэклога приведён в согласие с тем, что команда кладёт на самом деле. Развилку решал владелец: разбор triage показал, что ход из карточки — исключить одну строку «Области», как сделано для `minor/` — оставил бы четыре ошибки из пяти и собственный симптом не закрыл.

**Проверки.** `node bin/backslop.js gates --require-clean` на `aab1bad` — код 0, «гейтов 2, зелёных 2»; `lint` код 0, `npm test` код 0, tests 284 / pass 284 / fail 0. Против базы `1330e9e` (281 вердикт) снятых имён ноль. Новая проба требует красноты от **каждой** заглушки карточки в `queue/` и зелени от неё же в `triage/`. Мутационная проба: `lib/lint.js:285` (`if (false) continue;`) — `npm test` код 1, 284/282/0, покраснели ожидавшийся «lint: 4. карточка new в triage/…» и «new: дробный parent…». Изолированное ревью в двух проходах, находки закрыты.

**Отдельно — подмена смысла внутри сохранённого имени.** `test/commands.test.mjs:59` (`new: дробный parent принимает находку и сохраняет связь в поле Родитель`) раньше требовал, чтобы незаполненная «Улика» в `triage/` красила `lint`; теперь требует зелени в `triage/` и красноты после `mv 7.2 queue`. Имя не менялось, поэтому разность множеств имён этого не показывает — смена контракта санкционирована владельцем.

**Доки тем же ходом.** `docs/backlog/README.md` и оба шаблонных слоя (`templates/docs/backlog/README.md`, `templates/en/docs/backlog/README.md`), `docs/reference/03-lint.md`, `README.md` и `README.ru.md` — в обоих README описание `lint` несло прежний контракт, и нашло это ревью, а не гейт: гейт 11 смотрит в README только пин версии. `CHANGELOG.md` — запись в секции v0.10.0.

**Находка.** [BS-64.1](../../backlog/queue/BS-64.1-minor-evidence-placeholder-fails-lint-at-birth.md) — карточка, заведённая `new --parent N --minor`, краснит `lint` в `minor/` с рождения: тот же класс, что закрыт здесь для `triage/`. Заведена с живым замером, ход выбирает владелец.
