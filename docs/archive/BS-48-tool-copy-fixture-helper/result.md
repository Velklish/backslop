# BS-48 · Результат

**Закрыта 2026-09-09.** Выполнена. `toolCopy(mutate)` и `toolCli(tool, args, { cwd, env })` живут в `test/helpers.mjs` рядом с `makeProject` и `cli`; запуск бинаря — общий `runBin` с тем же окружением (без цвета, `--no-warnings`). `test/init.test.mjs` берёт их напрямую, `toolProject` в `test/lint.test.mjs` остался обёрткой «init в копии → мутация → lint» в четыре строки. Пробы BS-36.2 этого захода написаны сразу на общем helper'е.

**Проверки.** `grep -n "cpSync(path.join(REPO" test/*.mjs` — только `test/helpers.mjs`. Число тестов не изменилось: 233 до и после переноса в worktree. Мутационной пробы у переноса оснастки нет — поведение проверяемого не менялось; охраной служат сами тесты на копии инструмента (гейт парности, гейт 11, пробы BS-36 и BS-36.2), все зелёные. Гейты — как у BS-19.1.

**Доки тем же ходом.** [03-lint](../../reference/03-lint.md) — пробы гейтов репозитория инструмента идут на копии; `CHANGELOG.md`; карточка заполнена (разделы были заглушками).

**Ревью.** Общее с BS-19.1: находок по задаче нет; наблюдение reviewer'а — `test/release.test.mjs` держит свой `REPO` — вне области карточки, не тронуто.
