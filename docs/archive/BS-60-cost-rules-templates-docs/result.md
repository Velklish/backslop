# BS-60 · Результат

**Закрыта 2026-09-18, выполнена.** Правило маршрутизации находки по цене ([ADR-022](../../adr/adr-022-cost-decides-finding-fate.md)) записано во всех текстах, по которым работают агенты, ru и en одним составом: таблица каталогов и «Как вести» в `templates/docs/backlog/README.md` (шкала цены, ход для `critical`/`major`/`minor`/гипотезы, пачки в «Такт разбора triage»), подкаталог `minor/` пачки в `templates/docs/archive/README.md`, шаг 1 блока `AGENTS.md` (`templates/agents-section.md`), бриф (`templates/brief.md`), скиллы `backslop-task` (находки в результате worker'а, разбор triage) и `backslop-batch` (пачки в «До резки», находки под задачей N, границы worker'а). Шаблон ADR-001 называет пять каталогов статусов. Зеркала `docs/backlog/README.md` и `docs/archive/README.md` перегенерированы из шаблонов, блок `AGENTS.md` — командой `init`. Ссылка на ADR-022 из шаблона backlog README снята: в свежем проекте после `init` этого ADR нет, и гейт ссылок краснел в шести тестах.

**Проверки.** `node bin/backslop.js gates` на adef059: код 0, гейтов 2, зелёных 2; `npm test` 281/281; гейты 11–12 (парность ru/en, слоты шаблонов) зелёные. `test/brief.test.mjs` обновлён под новый текст брифа (пять ассертов ru/en). `diff` зеркал с отрендеренными шаблонами — только подстановки `{{cli}}` и `{{project}}` (сверено reviewer'ом построчно). Ревью: три круга, по этой карточке закрыты шаблон ADR-001 ru+en, два пути и фраза про гейты в ADR-022, снятая ссылка на прежнюю цитату, «Цена» в справочнике и «шапка» в глоссарии.

**Доки тем же ходом.** `README.md`, `README.ru.md` (статусы, находка, строки `new`/`mv`/`archive`/`status`, граница ролей), `docs/GLOSSARY.md` (термины «цена находки», «пачка», статус `minor`, шапка), `docs/adr/adr-022-cost-decides-finding-fate.md`, `docs/README.md` (строка ADR), `CHANGELOG.md`.
