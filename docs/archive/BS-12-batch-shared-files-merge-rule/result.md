# BS-12 · Результат

**Закрыта 2026-09-05.** Выполнена.

Раздел «Слияние и приёмка» скилла `backslop-batch` (ru и en) запрещает стратегии `-X theirs`/`-X ours` для файлов, общих для track'ов, и задаёт сборку из двух редакций по единицам структуры: CHANGELOG — объединение пунктов по заголовку `- **…**`, документ с заголовками — объединение секций, список в коде — руками с проверкой имён обеих сторон грепом; мерило — пустой `git diff HEAD <голова worker'а> -- <файлы worker'а>` из пункта 3. В разделе резки очереди — правило владения находками: под задачей N до слияния заводит только worker её track'а, оркестратор — в записи приёмки или задачей без родителя; причина названа (BS-11 не видит чужой клон и неподтянутую ветку). Проверено: `lint` зелёный (равенство шаблонов ru/en); `init --tools claude,cursor,codex` во временном проекте рендерит новый текст во все три adapter output, `lint` там зелёный.

**Доки тем же ходом.** CHANGELOG. Справочник не трогался: правило живёт в скилле, на CLI не влияет.
