# BS-6 · Результат

**Закрыта 2026-09-05.** Выполнена.

`id` задачи хранит форму из имени файла, `canonicalId` даёт числовую форму для сравнения; гейты номеров и упоминаний сравнивают числом, дубль в разных формах записи — ошибка; находка наследует форму родителя. Проверено: зелёная проба с нулями в архиве и упоминаниями в обеих формах, красная проба дубля, команды `new --parent` и `mv` на файле с нулями (`test/lint.test.mjs`, `test/commands.test.mjs`). На копии `ati-agents` (95 каталогов с нулями, ~700 упоминаний) ошибки этого класса ушли целиком.
