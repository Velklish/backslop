# BS-16 · Результат

**Закрыта 2026-09-09.** Выполнена. `package.json`: `"test": "node --test --test-timeout=60000"` — висящий тест держит прогон минуту, а не вечность; у теста про пайп в `test/review.test.mjs` свой `{ timeout: 20000 }` с комментарием, почему это не дубль глобального флага: свой лимит спасает прямой запуск файла, глобальный — остальные тесты набора, у которых своего нет. Решение `exitCode` в `bin/backslop.js:108` не тронуто.

**Проверки.** Worker: `npm test` — 142 из 142, код 0, 25,6 с; самый долгий тест набора 4,9 с, запас к лимиту двенадцатикратный; `lint` — 0 ошибок. Актуализация: мутация `exitCode → process.exit(code)` на копии HEAD — четвёртый тест `review.test.mjs` не завершался, снят сторожем на 60-й секунде. Мутационные пробы: та же мутация с прямым `node --test test/review.test.mjs` — exit 1 за 24 с, «test timed out after 20000ms», остальные четыре теста доехали; заведомо висящий тест без своего лимита — старая форма снята сторожем на 45-й секунде, новая — exit 1 за 60 с. Оговорка: снятый по таймауту тест Node считает отдельной категорией, в `pass`/`fail` он не входит, код возврата 1. На `main` после приёмки: `npm test` — 145 из 145, код 0; `lint` — код 0.

**Доки тем же ходом.** Не потребовалось: имя гейта `npm test` и решение `exitCode` в [02-cli](../../reference/02-cli.md) не изменились.

**Ревью.** Дифф в две строки без кода продукта — принят оркестратором без изолированного ревью.
