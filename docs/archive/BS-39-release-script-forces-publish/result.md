# BS-39 · Результат

**Закрыта 2026-09-09.** Выполнена. `npm run release -- X.Y.Z --no-publish` — штатный релизный путь до закрытия BS-2.1: флаг снимает ровно шаг `npm publish`, локальный тег, `push --dry-run` и atomic push `main` с тегом остаются; отказ после тега различает «`backslop@X.Y.Z` опубликован» и «npm publish не запускался (--no-publish)»; `--bump` вместе с `--no-publish` — отказ: бамп до релиза не доходит и ничего не публикует. `AGENTS.md` называет форму с `--no-publish` релизным путём с привязкой к условию возврата BS-2.1 (владелец выбрал версию и подтвердил npm auth/2FA) и говорит прямо: это штатный путь, а не обход скрипта; форма без флага включится по возврату BS-2.1. Абзац «Релиз» в `AGENTS.md` переписан целиком, смысловых утверждений сверх карточки не добавлено.

**Проверки.** Актуализация: скрипт принимал один позиционный аргумент, `npm publish` стоял безусловно между `git tag` и push; строки сдвинуты BS-29, появился `--bump`, с которым флаг пришлось развести отказом. Worker: `npm test` — 170 из 170, код 0; `lint` — 0 ошибок; `gates` — 2 из 2. Мутационные пробы: ветка `--no-publish` снята (`publish = true`) — 1 красный; условие несовместимой пары → `if (false)` — 1 красный. Живого прогона релиза worker не делал — теги и сеть запрещены брифом; путь проверен фикстурой с шимами `git`/`npm` (порядок вызовов, отсутствие `npm publish`), поведение настоящего `git push --atomic` проверится первым релизом 0.5.0 оркестратором. На `main` после приёмки track'а: `npm test` — 224 из 224, код 0; `lint` — код 0.

**Доки тем же ходом.** `AGENTS.md` — абзац «Релиз»; [02-cli](../../reference/02-cli.md) — абзац релиза; `CHANGELOG.md`.

**Ревью.** Изолированное ревью (reviewer Claude, база `da82f7b`): одно minor — документированный отказ пары флагов без пробы; закрыто строкой в таблице отказов, сверено оркестратором по диффу.
