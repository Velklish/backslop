# BS-19.1 · Результат

**Закрыта 2026-09-09.** Выполнена. Выбрана охрана в `scanTasks`, а не порядок гейтов: каталог с именем по шаблону файла задачи — не задача. `isFileEntry` (`lib/tasks.js`) отбрасывает каталог по `Dirent`, файл принимает, остальное (symlink, неизвестный тип записи) доразрешает `statSync`; та же охрана в `findFlatTask`, иначе `mv 9 queue` на каталоге `docs/backlog/BS-9-sub.md` сначала переносил каталог, а потом падал на чтении. Гейт 2 `lint` для symlink берёт `stat`: ссылка на каталог получает диагностику каталога, битая ссылка — свою («битая ссылка в каталоге статуса»).

**Проверки.** Пробы `2. каталог, названный как файл задачи, в каталоге статуса`, `2. symlink на каталог с именем файла задачи — та же диагностика`, `2. битая ссылка с именем файла задачи в каталоге статуса` и CLI-тест `lint: каталог с именем файла задачи — диагностика гейта 2 в stderr, а не стек EISDIR` (`test/lint.test.mjs`); тест `mv: каталог с именем файла задачи в плоском docs/backlog/ — отказ без переноса и без стека` (`test/commands.test.mjs`). Мутационные пробы после коммита (`node --test --test-name-pattern`): снять охрану в `scanTasks` — 2 из 2 красных; снять охрану в `findFlatTask` — 1 из 1 красный; снять ветвь битой ссылки в гейте 2 — 1 из 1 красный. В worktree перед приёмкой: `gates` — код 0, гейтов 2, зелёных 2; `npm test` — 237 из 237; `lint` — 0. На `main` после приёмки: `gates` — код 0, гейтов 2, зелёных 2; `npm test` — 237 из 237; `lint` — 0.

**Доки тем же ходом.** [03-lint](../../reference/03-lint.md) — гейт 2: подкаталог, symlink на каталог, каталог с именем по шаблону, битая ссылка; `CHANGELOG.md`.

**Ревью.** Изолированное ревью (reviewer Claude на шине, пять кругов на общий дифф worktree с BS-36.1, BS-36.2, BS-48, BS-32.1). По этой задаче: три minor первого круга — фраза `03-lint` шире кода (сужена), symlink на каталог молчал (stat в гейте 2), охрана `findFlatTask` без пробы (проба добавлена); minor второго круга — битая ссылка не названа ни одним гейтом (третья ветвь гейта 2). Все закрыты.
