# BS-19 · Результат

**Закрыта 2026-09-09.** Выполнена. Пять ветвей `lint` (каталог внутри каталога статуса; отсутствующий `docs/backlog/`; посторонний файл в `docs/archive/`; каталог архива без `task.md`; ADR при удалённом индексе документации) и четыре проверки формы `loadConfig` (`prefix` не по `PREFIX_RE`, `docs` пустой, абсолютный или с `..`, `cli` из пробелов, `gates` не массив или с числом) получили красные пробы: пять `probe(...)` отдельным блоком в конце `test/lint.test.mjs`, один тест с девятью `assert.throws` в `test/config.test.mjs`. `lib/lint.js` и `lib/config.js` не тронуты. CHANGELOG — запись по требованию карточки: правило AGENTS.md и текст 03-lint про красную пробу стали фактом для этих ветвей; полного перебора всех `err()` в `lib/lint.js` задача не делала — это её граница.

**Проверки.** Актуализация: с отключёнными всеми девятью ветвями `npm test` был зелёный — 148 из 148 (карточка мерила 134 из 134); номера строк в `lib/lint.js` уехали на +1…+10 после BS-33, в `lib/config.js` совпали. Worker: `npm test` — 169 из 169 после merge main, код 0; `lint` — 0 ошибок. Мутационные пробы по каждой из девяти ветвей порознь: каждая красит ровно один тест (lint: 43 pass / 1 fail пять раз; config: 4 pass / 1 fail на четырёх разных строках ассерта); пять проб lint перепрогнаны после merge main с гейтом 11 — те же пять красных. На `main` после приёмки: `npm test` — 180 из 180, код 0; `lint` — код 0.

**Доки тем же ходом.** `CHANGELOG.md`; reference не менялся.

**Ревью.** Изолированное ревью (reviewer Claude, база `aa36f3b`): по BS-19 находок нет; шесть находок того же отчёта относятся к уже принятым задачам track'а C (гейт 11, команда `gates`) и к гармонизации и переданы владельцам. Находка worker'а BS-19.1 (`lint` падает с EISDIR на каталоге с именем задачи — `scanTasks` читает его как файл раньше, чем гейт 2 дойдёт до своей ветви) — в очереди.
