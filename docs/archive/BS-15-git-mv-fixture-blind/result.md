# BS-15 · Результат

**Закрыта 2026-09-09.** Выполнена. Оснастка `run()` в `test/helpers.mjs` бросает при ненулевом коде git с причиной из stderr, затем stdout («нечего коммитить» git пишет в stdout), затем `error.message`, затем кодом; экспортирована — тестам нужен `git status`. `makeProject` пинит `status.renames=true` и `commit.gpgsign=false` рядом с личностью: строгий `run()` иначе падал бы от глобального конфига участника. Три теста в `test/archive.test.mjs` различают ветки `moveFile`: файл в индексе переезжает через `git mv` (в `git status --porcelain` строка `R old -> new`, stderr пуст); файл вне репозитория — переименованием с предупреждением «файл не в индексе git — перенесён без git mv»; файл внутри репозитория, но не в индексе — то же предупреждение, строки `R` нет. `lib/tasks.js` и `lib/util.js` не тронуты: тесты написались снаружи.

**Проверки.** Worker: `npm test` — 142 из 142, код 0; `node bin/backslop.js lint` — 0 ошибок, код 0. Актуализация: мутация `tracked = { status: 1 }` на прежнем наборе давала 139 из 139 зелёных — ни один ассерт ветку не отличал; предупреждение после BS-43 живёт в `lib/tasks.js:467`, не в `mv.js`/`archive.js`. Мутационные пробы после коммитов: `tracked → { status: 1 }` — 1 красный на тесте git mv; `tracked → { status: 0 }` — 2 красных на fs-тестах; предупреждение обезврежено — 1 красный; подложен неверный флаг в `gitAll` — со строгим `run()` 4 красных с текстом git, с прежним — 1 без причины; пин перевёрнут на `status.renames=false` — 1 красный, улика: без пина `-c status.renames=false` печатает `A` + `D` вместо `R`. На `main` после приёмки: `npm test` — 145 из 145, код 0; `lint` — код 0.

**Доки тем же ходом.** Не потребовалось: поведение не менялось, [02-cli](../../reference/02-cli.md) уже описывает обе ветки; CHANGELOG для тестовых задач не трогается.

**Ревью.** Изолированное ревью (reviewer Claude, база `890d490`): три minor — зависимость от `status.renames` глобального конфига, потерянная причина «нечего коммитить», ветка fs проверена вырожденным маршрутом; закрыты коммитом review fix, закрытие сверено оркестратором по диффу.
