# BS-18 · Результат

**Закрыта 2026-09-09.** Выполнена. `backslop.json` самого backslop: `gates` — `["node bin/backslop.js lint", "npm test"]`; `AGENTS.md:7` зовёт `node bin/backslop.js gates` и называет, что он гонит. Дефолт `gates` в `lib/config.js` не тронут — у чужого проекта npm-тестов может не быть. CHANGELOG не трогается: состав self-host конфига для потребителя не наблюдаем.

**Проверки.** Живой прогон `node bin/backslop.js gates` до и после: «гейтов 1, зелёных 1» → «гейтов 2, зелёных 2», код 0; порознь `lint` — 0 ошибок, `npm test` — 151 из 151. Мутационной пробы нет: правка — состав конфига, кода и теста не содержит. На `main` после приёмки: `gates` — код 0, «гейтов 2, зелёных 2».

**Доки тем же ходом.** `AGENTS.md` — строка гейтов; [01-layout](../../reference/01-layout.md) описывает дефолт для чужих проектов и правки не потребовал.

**Ревью.** Одно поле конфига и строка AGENTS.md — принято оркестратором без изолированного ревью; правка AGENTS.md пришла из находки reviewer'а по BS-14.
