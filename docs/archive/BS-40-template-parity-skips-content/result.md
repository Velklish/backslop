# BS-40 · Результат

**Закрыта 2026-09-09.** Выполнена. `templateParity` сверяет сверх состава файлов и подстановок ещё три вещи, механически и без перевода: контракт фронтматтера `skills/*/SKILL.md` — `name` и `description` непусты в обоих слоях и `name` равен имени каталога скилла; число и уровень заголовков совпадают между ru- и en-версией, при этом заголовки считаются по тексту с погашенными блоками кода (`blankFences`), иначе `# ` в примере команды считался бы заголовком; в файлах под `templates/en/` нет кириллицы (`[А-Яа-яЁё]`, тот же диапазон, что в тестах init) — по всем файлам слоя, не только по парам. `placeholders` принимает текст, файл читается один раз на пару. Ограничение названо в 03-lint: расхождение содержания, не сдвинувшее число заголовков, гейт не ловит — это остаётся на ревью; `name` сверяется с каталогом, отдельного сообщения «name слоёв разошлись» нет.

**Проверки.** Замер до правки: 18 пар шаблонов, расхождений заголовков 0, кириллицы в `templates/en/` 0, фронтматтер у шести SKILL.md полон — гейт расширен на зелёном дереве. Три новых теста были красными до правки (2/5). Worker: `npm test` — 145 из 145, код 0; `lint` — 0 ошибок. Мутационные пробы после коммита: вырезан `skillFrontmatter` — 4/5; вырезано сравнение заголовков — 4/5; вырезана проверка кириллицы — 4/5; `blankFences` убран из счёта заголовков — 4/5 (фенсы проверены отдельно, а не заодно). На `main` после приёмки track'а: `npm test` — 201 из 201, код 0; `lint` — код 0.

**Доки тем же ходом.** [03-lint](../../reference/03-lint.md) — пять сверок поимённо и ограничение; [01-layout](../../reference/01-layout.md) — требования к английскому слою; `CHANGELOG.md`.

**Ревью.** Изолированное ревью (reviewer Claude, база `b127b98`): находок нет.
