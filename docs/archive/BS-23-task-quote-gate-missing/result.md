# BS-23 · Результат

**Закрыта 2026-09-09.** Выполнена. Гейт 10 «Цитата живого файла»: блок `<!-- quote:<путь> -->…<!-- /quote -->` в карточке (путь резолвится как ссылка — относительный от файла, корневой от корня проекта) сверяется с непрерывным куском файла по строкам; обёртка-фенс (ведущая и замыкающая строки) снимается, фенсы внутри цитаты остаются, пробелы по краям срезаны с обеих сторон; три ошибки — файла нет или это не файл, цитата разошлась (файл и первая строка), блок не закрыт (и когда следующий открывающий маркер пришёл раньше закрывающего). Маркеры ищутся по тексту с погашенными блоками кода, тело — по сырым строкам: показанная в фенсе форма остаётся примером. `docs/archive/` из обхода исключён — снимок момента. Пункт карточки про шаблон `task.md` мёртв: улики с номером строки в шаблоне не было никогда (один коммит affdef0); вместо него форму улики называет строка `context` находки в `lib/new.js` (RU/EN). Карточка называла гейт «девятым» — вышел десятым: девятый занял BS-33.

**Проверки.** Worker: `npm test` — 150 из 150 (+4), затем 158 из 158 после review fix, код 0; `lint` — 0 ошибок по гейту на живом дереве (блоков цитат в `docs/` ещё нет). Мутационные пробы по пяти ветвям порознь — каждая красит свой тест; снятие фильтра архива и замена `blankFences` сырым текстом — красный зелёный-тест; отдача незакрытого блока снята — 2 красных; возврат к вырезанию всех фенсов из иглы — 7 красных, включая зелёный проект (фикстура цитирует кусок с блоком кода внутри). Наивный перебор в `quoted` — предположение без замера. На `main` после приёмки: `npm test` — 173 из 173, код 0; `lint` — код 0.

**Доки тем же ходом.** [03-lint](../../reference/03-lint.md) — ряд 10 и границы (якоря и диапазоны строк не проверяются, хвостовые пробелы срезаются); `CHANGELOG.md`; `lib/new.js` — текст улики находки.

**Ревью.** Изолированное ревью (reviewer Claude, база `116d424`): пять замечаний — незакрытый блок глотался вторым маркером, фенсы внутри цитаты давали ложный красный, `trim`, каталог вместо файла, порядок проб; закрыты коммитом review fix, закрытие сверено оркестратором по диффу.
