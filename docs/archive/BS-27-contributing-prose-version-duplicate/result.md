# BS-27 · Результат

**Закрыта 2026-09-06.** Слита в [BS-24](../../backlog/queue/BS-24-prose-pin-not-rewritten.md). Предмет один: `upgrade` переставляет пин только в `cli` и `gates`, а прозу в markdown не трогает, и `lint` расхождения не видит. Улика этой записи (`docs/guides/contributing.md:3` в promptobus называет `v0.3.0` прозой, а строки 6-7 того же файла дают команды с `#v0.4.0`) перенесена в BS-24 отдельным пунктом «Что сделать»: расхождение внутри одного файла между прозаическим номером и пином в команде не покрывается заменой по литералу `cli`, и решение — второй шаблон поиска или явный отказ, названный в reference. Правки самого promptobus эта запись не требовала.

**Проверки.** Слияние, кода не менялось: `node bin/backslop.js lint` зелёный на дереве после переезда.

**Доки тем же ходом.** Не потребовалось — запись закрыта слиянием, поведение инструмента не менялось.
