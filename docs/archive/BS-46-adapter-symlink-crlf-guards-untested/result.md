# BS-46 · Результат

**Закрыта 2026-09-09.** Выполнена. Четыре ветви, объявленные комментариями кода, закреплены тестами: охрана `ownedPath` от записи сквозь symlink (`test/init.test.mjs`: `.claude` → symlink на соседний каталог, `init --tools claude` отказывает кодом 1 с текстом «adapter path содержит symlink», за ссылкой ничего не появляется); следование по symlink в обходе markdown (`test/mdwalk.test.mjs`: `docs/link/b.md` за ссылкой на каталог попадает в обход, петля закрыта соседним тестом); терпимость owned-маркера к CRLF (`test/adapter-ownership.test.mjs`: CRLF с фронтматтером и без, плюс ассерт позиции маркера после `---\r\n` — без него порча регекса фронтматтера в `markGenerated` проходила незамеченной); откат `moveFile` на `renameSync` с предупреждением закрыт в BS-15 двумя тестами и здесь не дублируется. Symlink-тесты пропускаются на Windows, как принято в наборе. Код продукта не тронут.

**Проверки.** Worker: `npm test` — 145 из 145, код 0 (148 после merge main); `lint` — 0 ошибок. Мутационные пробы после коммита: охрана symlink обезврежена — 1 красный; убран `\r?` из `GENERATED_AT` — 1 красный; убран `\r?` из регекса фронтматтера в `markGenerated` — 1 красный на ассерте позиции; следование по symlink заменено пропуском — 1 красный. Уточнение к «Проверкам» карточки: CRLF-текст без фронтматтера к `\r?` нечувствителен (маркер ставится с `\n`), поэтому обещанное карточкой «новые кейсы покраснеют» во множественном числе неверно — краснеет только фронтматтерный; кейс без фронтматтера оставлен как фиксация заявленного поведения. Актуализация: предупреждение fs-отката после BS-43 живёт в `lib/tasks.js`, не в `mv.js`/`archive.js`; `ownedPath` — `lib/adapters.js:25-44`. На `main` после приёмки: `npm test` — 163 из 163, код 0; `lint` — код 0.

**Доки тем же ходом.** Не потребовалось: поведение не менялось; CHANGELOG для тестовых задач не трогается.

**Ревью.** Изолированное ревью (reviewer Claude, база `c8c8387`): по BS-46 находок нет; семь находок того же отчёта относятся к уже принятой BS-33 и к гармонизации захода и переданы их владельцам.
