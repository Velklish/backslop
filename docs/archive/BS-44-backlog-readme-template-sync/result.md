# BS-44 · Результат

**Закрыта 2026-09-09.** Выполнена по варианту карточки: в `AGENTS.md`, в абзац «шаблоны — источник», добавлено правило — правка процессного текста в `templates/docs/backlog/README.md` или `templates/docs/archive/README.md` тем же коммитом обновляет одноимённый файл в `docs/` этого репозитория; только эта пара рендерится из шаблона 1:1, остальные файлы `docs/` осознанно наполнены проектным контентом. Автоматический гейт сверки отклонён по «Не входит» карточки как несоразмерный двум файлам одного репозитория; бриф worker'а ошибочно звал гейт, оркестратор вернул задачу к карточке.

**Проверки.** Актуализация посылки worker'ом на HEAD: рендер шаблона с подстановками против файла — `docs/backlog/README.md` 46 из 46 строк, расхождений 0; `docs/archive/README.md` 8 из 8, расхождений 0. `node bin/backslop.js lint` — 0 ошибок, код 0. Мутационной пробы нет: кода и теста правка не содержит, проверка карточки — дифф коммита в одну строку `AGENTS.md`. На `main` после приёмки: `lint` — код 0.

**Доки тем же ходом.** Сама правка и есть документация; CHANGELOG не трогается — правило репозитория в пакет не уезжает.

**Ревью.** Дифф в одну строку без кода — принят оркестратором без изолированного ревью.
