# BS-107.1 · removeFile refuses a non-file that marker-only ownership never passes to it

- **Scope:** [01. Layout](../../reference/01-layout.md) § What init lays down
- **Created:** 2026-09-26
- **Parent:** BS-107
- **Cost:** minor

## Evidence

Finding discovered while working on BS-107.

Evidence: Mutation on b2bfd75: lib/adapters.js:137 'if (!lstatSync(file).isFile()) {' -> 'if (false) {'; npm test rc=0, tests 599, pass 599, fail 0. cleanupAdapters calls removeFile only after isOwnedAdapterFile(rel, file) is true, and hasGeneratedMarker returns false unless lstatSync(file).isFile() (lib/adapter-ownership.js:37), so the refusal is reachable only if the file changes type between the two lstat calls. BS-107 kept it on purpose; the adapter ADR card places the symlink guard before removeFile.
