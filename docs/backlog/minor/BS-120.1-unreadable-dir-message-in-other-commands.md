# BS-120.1 · Unreadable directory: a bare refusal outside lint, a stack trace from the task scan

- **Scope:** [02. CLI](../../reference/02-cli.md)
- **Created:** 2026-09-26
- **Parent:** BS-120
- **Cost:** minor

## Evidence

Finding discovered while working on BS-120.

Evidence: HEAD 8864841: in a fresh 'init --tools none --lang en' project with 'chmod 000 docs/locked src/locked', 'node bin/backslop.js upgrade --dry-run' -> rc=1, stderr '✖ <abs project>/docs/locked: EACCES' (absolute path, no words, no tr); 'lint' on the same project -> rc=1 '✖ docs/locked: the directory is not readable (EACCES) — lint cannot walk it; …'. Only lib/lint.js run() words UnreadableDir (lib/mdwalk.js class UnreadableDir); repoMarkdown callers lib/tasks.js:580 and lib/fold.js:403 and upgrade's livePinFiles/stalePins callers get the bare message. Base 7660233 printed a stack trace for all of them.

Evidence (scanTasks), HEAD 683e3af: in a fresh 'init --tools none --lang en' project, 'chmod 000 docs/backlog/queue' -> 'lint' rc=1 with an uncaught 'Error: EACCES: permission denied, scandir '<abs>/docs/backlog/queue'' at scanTasks (lib/tasks.js:141) <- lintProject (lib/lint.js:50); 'status' rc=1 with the same error. 'chmod 000 docs/archive' gives the same for both commands. scanTasks lists the status directories, docs/archive and archive/<id>-<slug>/minor with bare readdirSync (lib/tasks.js:133, :141, :152), so UnreadableDir never forms there. lint, status, mv, archive and fold all call scanTasks before any markdown walk.

Evidence (upgrade across versions), HEAD 683e3af: a project with cli 'npx github:me/proj#v0.1.0', stamp 0.1.0, a local releases repo (v0.1.0, v0.11.1) as source, an npx shim onto this checkout, and 'chmod 000 src/locked'. 'upgrade' prints the probe, migrate and init steps and saves the new pin (backslop.json cli '#v0.11.1', version 0.11.1). It then exits rc=1 with only '✖ <abs>/src/locked: EACCES' from rewriteProsePins (lib/upgrade.js:209). The afterPin recovery hint (lib/upgrade.js:201-203) is not printed, although the upgrade row of docs/reference/02-cli.md promises it. This is not a regression: base 7660233 printed a stack trace at the same step.
