# BS-120.2 · upgrade rewrites a pin in a LOG.md journal entry that follows a leading BOM

- **Scope:** [03. Lint gates](../../reference/03-lint.md) § gate 1
- **Created:** 2026-09-26
- **Parent:** BS-120
- **Cost:** minor

## Evidence

Finding discovered while working on BS-120.

Evidence: HEAD 8864841, scratch project: docs/archive/LOG.md = UTF-8 BOM + '- <a id="bs-1"></a>`BS-1-x` · 2026-01-01 · completed · — · Measured with `npx github:me/proj#v0.1.0 lint`' (journal entry on line 1). Node snippet with form parseCli('npx github:me/proj#v0.2.0'): mdwalk.stalePins(...) -> 0 hits (readText strips the BOM, isJournalLine matches), upgrade.rewriteProsePins(..., 'v0.2.0') -> ['docs/archive/LOG.md'] (livePinText keeps the BOM, LOG_ENTRY_HINT /^- <a id=/ misses, the journal pin is rewritten). Lint and the stale check skip it; the rewrite does not, so any other stale pin that triggers upgrade rewrites this history. Assumption: a real LOG.md starts with a '# ' header, so reach is limited to hand-made logs.

Update after the BS-120 review fix (the upgrade check reads through livePinText again, as on base 7660233): the same scratch LOG.md gives 0 hits for stalePins with its default reader (lint) and 1 hit with the upgrade check's reader (livePinText, BOM kept). Upgrade on the same version alone therefore goes on and rewrites the journal pin, as on base, and lint does not report it. The asymmetry is now lint vs upgrade, not the check vs the rewrite.
