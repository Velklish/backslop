# BS-84.2 · pinre-matches-version-prefix

- **Scope:** [02. CLI](../../reference/02-cli.md)
- **Created:** 2026-09-25
- **Parent:** BS-84
- **Cost:** minor (hypothesis)

## Evidence

Finding discovered while working on BS-84.

Evidence: Measured at 62eb0f0 (node -e in the worktree): rewriteGates(['npx github:me/proj#v0.1.0x lint', 'npx github:me/proj#v0.1.0-rc.1 lint'], 'npx github:me/proj#v0.1.0', parseCli(...), '0.2.0') returns ['npx github:me/proj#v0.2.0x lint', 'npx github:me/proj#v0.2.0-rc.1 lint']; pinRe(form) matches 'github:me/proj#v0.1.0' inside 'github:me/proj#v0.1.0-rc.1'; parseCli('npx github:me/proj#v0.1.0-rc.1') is null. pinRe (lib/config.js) has no end boundary, so upgrade's prose and gate rewrite move the version prefix of a suffixed or pre-release pin and keep the suffix, and lint compares only the prefix. Assumption, not verified: backslop has no pre-release tags (listReleaseTags keeps only vX.Y.Z), so the cost is a mangled pin in hand-written text; test/upgrade.test.mjs 'rewriteGates: every pin …' asserts the current behaviour.
