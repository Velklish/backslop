# BS-99.1 · mv N minor accepts Evidence that gate 4 rejects

- **Scope:** [02. CLI](../../reference/02-cli.md) § mv
- **Created:** 2026-09-25
- **Parent:** BS-99
- **Cost:** minor

## Evidence

Finding discovered while working on BS-99.

Evidence: lib/mv.js minorForm reads Evidence stubs with isTodoPlaceholder (whole-line rule: bullet only), gate 4 (lib/lint.js isPlaceholderLine) also reads numbered items, task boxes and table cells; a card whose only Evidence line is '1. [TODO: command]' passes 'mv N minor' and then fails 'lint' with 'line N: the [TODO] placeholder remains'

Measured on 7223703 in a throwaway `init --lang en --tools none` project: a triage card whose only Evidence line is `1. [TODO: command]` → `mv 1 minor` rc=0 (`✔ BS-1: triage/ → minor/`), then `lint` reports `line 7: the [TODO] placeholder remains` and `“Evidence” section is incomplete: [TODO] remains`. `mv` checks Evidence with `isTodoPlaceholder` (a bullet or a bare line only); gate 4 also reads numbered items, task boxes and table cells. `lint` catches the card, so no wrong state goes unnoticed; `mv` just accepts a card the next gate refuses.
