# BS-84.3 · npm-form-prose-backslop-reads-as-pin

- **Scope:** [02. CLI](../../reference/02-cli.md)
- **Created:** 2026-09-25
- **Parent:** BS-84
- **Cost:** minor (hypothesis)

## Evidence

Finding discovered while working on BS-84.

Evidence: Measured at 55bfa91: for an npm-form cli (npx backslop@X.Y.Z) samePinned in lib/migrate.js:98-105 builds the floating pattern from the spec 'backslop' and replaces every whole-word occurrence with the pin on both sides; node -e with that pattern turns 'the backslop tool and `npx backslop status`' into 'the backslop@0.11.0 tool and `npx backslop@0.11.0 status`' (a word followed by ':' or '.' is not matched). Both compared texts are normalised alike, so the comparison only gets more lenient. Assumption, not verified: the only miss is a local edit to the rules pair that changes nothing but 'backslop' to 'backslop@X' or back, which migrate would take for untouched output and redraw at the tool's own version; the GitHub form is unaffected because its spec 'github:owner/repo' does not occur as prose.
