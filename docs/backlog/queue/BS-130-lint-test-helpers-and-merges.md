# BS-130 · lint.test.mjs: greenProbe and toolProbe helpers, one git-shim table

- **Order:** 480
- **Scope:** [03. Lint gates](../../reference/03-lint.md)
- **Created:** 2026-09-25
- **Dependencies:** BS-95, BS-89

## Context

Line numbers are at commit 6f6318e (v0.11.0); if a line moved, find the test by the quoted name. A *mutation probe* means: edit the named lib line in the working tree, run the named test file with `node --test --test-timeout=60000 <file>; echo rc=$?`, read the red test names (lines starting with `✖`), then restore with `git checkout -- lib bin scripts templates`. Base run at 6f6318e: full suite 450 tests, 450 pass. Bug and code cards earlier in the queue changed some of the lib lines named in the mutation probes; apply each probe to the same logic where it lives when you start.

Base run: `node --test --test-timeout=60000 --test-concurrency=1 test/lint.test.mjs` -> rc 0, 128 tests (87 of them generated by the `probe()` helper at :43-55).

**1. Nine green controls repeat one 10-line body.** verified — read + mutation probe. :93, :103, :169, :182, :192, :225, :264, :380, :648 are each `makeProject({ git: false }); seedGreen; 1-3 put(); assert.deepEqual(problems(root), []); cleanup` and differ only in the `put()` payloads (11-15 ms each). They are real guards: M3 (drop `|| t.status === 'triage'` at lib/lint.js:397) turns all nine red.

**2. A hand-written red probe duplicates `probe()`.** verified — read. :203-212 is `seedGreen`, one `put` of `docs/backlog/deferred/BS-3-c.md`, `assert.match(problems(root).join('\n'), /без раздела «## Отложено»/)` — exactly `probe(name, mutate, expect)`.

**3. Six tool-copy red probes share one shape.** verified — read. :415, :433, :442, :455, :464, :585 are `let project; try { project = toolProject(mutate); assert.equal(project.code, 1, project.out); assert.match(project.err, RE); } finally { if (project) cleanup(project.dir); }` (230-340 ms each). Each guards a unique line: :415 lib/lint.js:238-241, :433 the stamp-naming message at lib/lint.js:94-96, :442 lib/lint.js:100-104, :455/:464 the github and npm alternatives of PIN_IN_PROSE (lib/lint.js:38), :585 lib/templates.js:78. :433 must stay as a row: deleting it was measured unsafe — dropping `${CONFIG_FILE} v${cfg.version}` from the message at lib/lint.js:96 with :433 skipped leaves the full suite green (449 pass, 0 fail); only :433 asserts `со штампом backslop\.json v\d+\.\d+\.\d+`.

**4. The two git-shim warning tests (:800, :820) share their whole setup.** verified — read + timing. Both: `makeProject`, shim tmpdir, `seedGreen`, `gitAll`, `foldCommitted`, `command -v git`, `lint` with the shim on PATH, exit 0; they differ only in the shim action (`kill -9 $$` vs `printf 'fatal: первая\nвторая\n' >&2; exit 128`) and the expected/forbidden lines (/оборван сигналом SIGKILL$/m, not /git null/; /fatal: первая$/m, not /вторая/). 980 ms and 677 ms, the two slowest tests in the file. Both rows are regression guards (signal wording; first-line cut at lib/lint.js:497).

**Must stay:** :57 (green baseline), :71 (EN), :279, :300, :334, :366 (only check of the `lint: ошибок N` summary), :392, :400 (tool-copy negative control), :433 (as a row), :482 and :496 (not merged: never mutation-probed), :513, :543, :564, :628, :691, :720, :764, :783, :840.

## Work to do

- Add `greenProbe(name, mutate)` next to `probe()` (:43-55): same body, assertion `assert.deepEqual(problems(root), [])`; turn :93, :103, :169, :182, :192, :225, :264, :380, :648 into one-line calls. Runtime test names stay as they are.
- Replace :203-212 with `probe('fenced-only заголовок секции не заменяет раздел', (root) => put(root, 'docs/backlog/deferred/BS-3-c.md', <same text>), /без раздела «## Отложено»/)` so the runtime name stays `lint: fenced-only заголовок секции не заменяет раздел`.
- Add `toolProbe(name, mutate, re)` beside `toolProject` (:475) and convert :415, :433, :442, :455, :464, :585 to one-line rows with unchanged names; :415 keeps its assertions inside `mutate`; :433 keeps its full regex `/версия package\.json v9\.9\.9 расходится со штампом backslop\.json v\d+\.\d+\.\d+/`.
- Merge :800 and :820 into one test (keep `skip: process.platform === 'win32'`): build the project and fold once, then loop rows `{ shim, expect, forbid }`, rewriting the shim file per row.

## Out of scope

- Deleting redundant probes, the vacuous minor/ probe fix and the live-pin split — BS-131 (`lint-test-redundant-probes`).
- Removing the `(?!\[TODO)` lookahead at lib/lint.js:24 (removal probe measured: suite stays green) — BS-131 (`lint-test-redundant-probes`) removes it.
- Merging :482 and :496 into one tool copy: never mutation-probed, saves ~240 ms only.
- Any change under lib/.

## Verification

- `node --test --test-timeout=60000 test/lint.test.mjs; echo rc=$?` -> rc 0, tests 127 (base 128: -1 for merging :800 and :820; the helper rows keep their runtime names).
- Mutation probe lib/lint.js:397 drop `|| t.status === 'triage'` -> all nine `greenProbe` rows red.
- Mutation probe lib/lint.js:96 drop `${CONFIG_FILE} v${cfg.version}` from the RU message -> the :433 row red.
- `npm test; echo rc=$?` -> rc 0.
