# BS-150 · One English ADR for the comment gate replaces ADR-028, ADR-035 and ADR-038

- **Order:** 680
- **Scope:** [Reference](../../reference/README.md)
- **Created:** 2026-09-25
- **Dependencies:** BS-140

## Context

Three ADRs describe one gate, `test/comment-length.test.mjs` run by `npm test` (package.json:19): `docs/adr/adr-028-two-line-comment-rule-and-its-gate.md` (two-line rule, the gate is a test, a shrink-only debt snapshot), `docs/adr/adr-035-comment-debt-swept-to-zero-probe-debtor-is-fixture.md` (debt swept to zero, the probe carrier is an unwalked fixture) and `docs/adr/adr-038-comment-line-width-under-gate.md` (line width <= 100 code points). This card replaces them with one English ADR. Line numbers are at base commit 6f6318e; the BS-140 (`comment-length-templates-tests`) card edited the same test file first (and removed the debt list), so find tests by their quoted names.

**How to write a consolidated ADR (applies to every ADR this card creates).** Create it with `node bin/backslop.js adr <slug> --title "<title>"` (the repository runs with `lang: en`, so the English template with Context / Options / Decision / Consequences is used and the number is the next free one; if another card takes the same number first, renumber on rebase: lint gate 8 refuses duplicate numbers). Header: `Status: Accepted`, `Date:` the day you write it, `Deciders: Velklish`. The text must not mention the numbers or file names of the ADRs it replaces, task or finding numbers, run ids, commit hashes, dated measurements or "owner decision of <date>" notes. Cite code by file and function name, not by line number (line numbers below are at base commit 6f6318e and only help you find the code). Options keeps only the rejected alternatives that still explain the choice, each with its cost in one sentence. Then delete the replaced ADR files and replace their rows in `docs/README.md` with one row for the new ADR (topic in English, Status equal to the Status line of the file; keep the table's current column layout). Every remaining reference to a deleted file is handled in the same card: links in `CHANGELOG.md` lose their link markup and old ADR tokens (the CHANGELOG rewrite drops them anyway); links and citations in `docs/reference/`, `docs/GLOSSARY.md` and `AGENTS.md` are dropped, not repointed (rules there carry no ADR citations); code and test comments that cite an old ADR get the new ADR id (`ADR-NNN`) and lose quoted section names the new ADR does not have — comments stay at most two lines and 100 code points per line (`npm test` enforces it).

**1. Every live rule of the three ADRs is implemented.** verified — read at the cited lines, and `node --test test/comment-length.test.mjs; echo rc=$?` -> `tests 16, pass 16, fail 0`, `rc=0`.
- Scope: `.js`/`.mjs` under `lib`, `test`, `bin`, `scripts` that git does not ignore — index plus untracked-not-ignored (`TREES` test/comment-length.test.mjs:14; `scannedCode` test/comment-scan.mjs:181-185 runs `git ls-files` and `git ls-files --others --exclude-standard`).
- Every tree must yield a file and the walk must read one (test/comment-scan.mjs:186-189; test `каждое дерево, названное гейтом, несёт код, который он может судить`, :72).
- Lexer with state across lines: quoted strings end at a newline, template literals nest through `${…}`, regex literals with classes and flags are skipped; `/` starts a regex unless the previous token ends a value (identifier, number, `)`, `]`, `}`, `++`, `--`), except after return, typeof, instanceof, in, of, new, delete, void, throw, case, do, else, yield, await (test/comment-scan.mjs:5-108).
- Block = run of consecutive lines carrying only comment; code before a comment makes a code line; code after a comment ends the block; a blank line ends it; `//`, `/* */` with bare continuation lines and JSDoc join one block (`commentBlocks`, test/comment-scan.mjs:165-178; lexer tests :194, :209).
- Length: a block longer than 2 lines fails, naming file, line and length (`LIMIT = 2`, :12; `longBlocks` test/comment-scan.mjs:193-197).
- Width: any block line wider than 100 code points fails — whole line with indent, a tab counts 1, a trailing `\r` is excluded (`const width = [...l.replace(/\r$/, '')].length;` test/comment-scan.mjs:204; `WIDTH = 100`, :13; boundary test `ширина: строка комментария в 101 знак — …`, :171-180).
- Probe carrier: `test/fixtures/comment-debtor.js.txt` holds a 3-line `//` block (L5-7), a 4-line `/* */` block (L10-13) and a 2-line block whose second line is 101 wide (L16-17); the `.txt` extension keeps it out of the walk and :131 asserts that. `node -e "import('./test/comment-scan.mjs').then(m=>{const t=require('fs').readFileSync('test/fixtures/comment-debtor.js.txt','utf8');console.log(JSON.stringify(m.longBlocks(t,2).map(b=>[b.line,b.length])),JSON.stringify(m.wideLines(t,100)))})"` -> `[[5,3],[10,4]] [{"line":17,"width":101}]`, rc=0.

**2. The debt list is gone.** The BS-140 (`comment-length-templates-tests`) card removed `PENDING`, the snapshot fixture `test/fixtures/comment-sweep-pending.json` and every helper and test that served only the list, after a removal probe. The ADR records the gate without a debt list.

**3. Stale statements not to carry.** verified — read.
- adr-028:44 counts "four refusals" of the debt list; the code has five separate checks (offenders :102-105, appeared :106-109, swept :110, unknown listed files :183, stale ids :184-191), and the probe test title `обход отказывает каждой из четырёх форм, ради которых существует` (:127) counts a different four. With the list gone only the offender refusal remains; the probe test title loses its count.
- adr-028:24 (rejected option 1B) argues from "thirteen lint gates"; lint has gate 14 since then (`// 14. Байт ниже 0x09 в исходнике` lib/lint.js:134). The new ADR keeps only the reason: lint runs in consumer projects that have neither `lib/` nor this rule, so the check is a test in this repository's `npm test`.
- adr-028:51/:53 and :66 carry the 42/180/705 counts and the old live-file debtor `lib/lint.js`; adr-028:5,11,13,15-17,70, adr-035:5,11, adr-038:5,11,30,42-43 carry run ids, card links, commit hashes and a comparison with another repository. None of it goes into the new ADR.
- docs/GLOSSARY.md:43 defines block identity as "sha256 of the block prose with indents removed; counted over the tree, not the file": ambiguous (matching is per file, :107) and inexact (`l.trim()` strips both ends, :24). Identity exists only for the debt list, so the sentence goes with it.

## Work to do

- Create the ADR (`node bin/backslop.js adr comment-length-gate --title "Inline comments: at most two lines and 100 code points, checked by a test"`). Context: agents read this code and pay for every comment paragraph on every read; a nuance that does not fit belongs in docs with a short pointer; the rule governs this repository only, and lint runs in consumer projects, so the check is a test; a line regex cannot find comments (a backtick in a regex literal opens a template that never closes), so the gate needs a lexer with state; a walk that reads nothing passes every emptiness check, so the gate proves it read files. Decision: the scope, lexer, block, length and width rules of item 1; no debt list — every block over the limit fails wherever it is, and a new file complies from its first save; the floor `judged == walked files`; the probe on the unwalked `.txt` fixture. Options: a lint gate (runs in projects without this rule); a shrink-only debt list (nothing left to shrink, and an empty list with zero ceilings only rejects). Consequences — not caught by design: prose (a false comment, one restating the code, one carrying a task number or date passes if it fits); a comment trailing a code line; a line where code follows a comment; a long comment split by blank lines into short blocks; composite emoji counted wider than displayed; East Asian wide characters and tabs counted as 1; a regex after `)` read as division. Renaming the fixture to `.js`/`.mjs` puts it into the walk and fails the not-walked assertion.
- Rewrite the fixture header comment (test/fixtures/comment-debtor.js.txt:1-2): it is the probe carrier, not a debt carrier, and cites the new ADR id. Repoint the other test citations: test/comment-length.test.mjs:1-2 (file header), :131 (assert message), :179 (assert message), :228; test/comment-scan.mjs:2, :199.
- Delete the three ADR files and replace their rows `docs/README.md:39`, `:46`, `:49` with one row for the new ADR.
- AGENTS.md:11 (Comments rule, whatever language it is in when you start): remove the sentence saying the accumulated debt lives in `test/fixtures/comment-sweep-pending.json` and can only shrink, and remove the two ADR links; keep the rest of the rule.
- docs/GLOSSARY.md:43 (`comment block`): drop the identity sentence and the ADR link from the evidence cell; keep the definition and the `test/comment-scan.mjs`, `commentBlocks` evidence.
- CHANGELOG.md:5 (the `ADR-038` token), :17 (link to adr-035), :42 (link to adr-028): remove the old ADR tokens and link markup, keep the rest of the entry text.
- Remove the task number used as origin from the nine comments the sweep found (measured on 6f6318e with the lexer in test/comment-scan.mjs over lib, test, bin, scripts): test/brief.test.mjs:193, test/commands.test.mjs:14 and :648, test/config.test.mjs:47, :75 and :138, test/gates.test.mjs:139, test/lint.test.mjs:688, scripts/release.mjs:85 — keep the technical content of each comment, drop the number; the other 21 task-number tokens in comments are fixture names and record forms, not origins, and stay.

## Out of scope

- Changing the lexer, the 2-line limit or the 100-code-point width.
- Catching origin notes or other prose problems in comments (the nine comments that still name a task number as their origin are handled by the work item below, not by the ADR text).
- The debt-list removal, the implied-assert deletions and the lexer/parity tables in the same test file: the BS-140 (`comment-length-templates-tests`) card (before this card).
- The English rewrites of AGENTS.md and the glossary (the BS-173 (`agents-md-english-rewrite`) card, the BS-174 (`docs-index-glossary-english`) card); they must not reintroduce a debt list or a block-identity term.
- Compressing CHANGELOG sections (the BS-175 (`changelog-compress-english`) card).

## Verification

- `git grep -n -E 'PENDING|comment-sweep-pending|blockId|staleIds|unknownFiles|unnamedBlocks' -- test lib docs AGENTS.md ':!docs/backlog' ':!docs/archive'; echo rc=$?` -> no output, `rc=1`.
- `git grep -n -E 'ADR-0(28|35|38)([^0-9]|$)|adr-0(28|35|38)-' -- . ':!docs/archive'; echo rc=$?` -> no output, `rc=1` (base: 17 lines outside docs/adr).
- `node bin/backslop.js lint; echo rc=$?` -> `rc=0` (gate 1 fails on any link to a deleted ADR file, gate 8 on an ADR without a `docs/README.md` link or a duplicate number).
- `npm test; echo rc=$?` -> `rc=0` (base 6f6318e: 450 tests, 450 pass).
