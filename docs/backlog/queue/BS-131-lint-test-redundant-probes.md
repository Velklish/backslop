# BS-131 · lint.test.mjs: drop redundant probes, fix the vacuous minor/ probe, split live-pin

- **Order:** 490
- **Scope:** [03. Lint gates](../../reference/03-lint.md)
- **Created:** 2026-09-25
- **Dependencies:** BS-130

## Context

Line numbers are at commit 6f6318e (v0.11.0); if a line moved, find the test by the quoted name. A *mutation probe* means: edit the named lib line in the working tree, run the named test file with `node --test --test-timeout=60000 <file>; echo rc=$?`, read the red test names (lines starting with `✖`), then restore with `git checkout -- lib bin scripts templates`. Base run at 6f6318e: full suite 450 tests, 450 pass. Bug and code cards earlier in the queue changed some of the lib lines named in the mutation probes; apply each probe to the same logic where it lives when you start.

Base run: `node --test --test-timeout=60000 --test-concurrency=1 test/lint.test.mjs` -> rc 0, 128 tests (87 of them generated by the `probe()` helper at :43-55).

**5. `lint: каталог с именем файла задачи — диагностика гейта 2 в stderr, а не стек EISDIR` (:844-856) is subsumed by the probe at :691.** verified — mutation probe, full suite with the test skipped. M5 lib/lint.js:282 `if (st?.isDirectory()) err` -> `if (false) err`: without :844 still red :689, :691, :692. M5b lib/tasks.js:197 `if (e.isDirectory()) return false;` -> `return true;`: without :844 still red :691 and the commands test at :650. Not probed: a mutation that changes only CLI stderr formatting; CLI error printing is covered by :366.

**6. The whole-template result.md probe call (:220, 2 runtime tests ru/en) adds no kill.** verified — mutation probe, with a caveat: lib/tasks.js:369 `hasResultTodo` -> `return false;` is still caught by the six per-paragraph probes (:221-223). Count-sensitive mutations (e.g. requiring two stubs) leave the per-paragraph probes green and are caught besides :220 only by test/init.test.mjs:63-64, i.e. for the RU template only. Accepted: every probed mutation stays killed.

**7. `lint: 2. номер занят дважды` (:130) is weaker than :392 and :840.** verified — mutation probe. M4 lib/lint.js:261 `if (prev) err(t.file` -> `if (false) err(t.file`: without :130 still red :392 (BS-004 vs archived BS-4) and :840 (log line vs archive dir); same for lib/lint.js:262 `else seen.set(key, t);` -> `else {}`. Not probed: a mutation firing only for exact same-spelling duplicates.

```text
**8. `lint: 5. каталог в minor/ пачки` (:144) is vacuous.** verified — mutation probe. lib/lint.js:438 is `if (!m.isFile() || !fileRe.test(m.name)) err(...)`; the probe creates the directory `BS-4.9-x` (no `.md`), which already fails `fileRe` like the notes.md probe at :143. Dropping `!m.isFile() ||` keeps the whole suite green (450/0). With the directory renamed to `BS-4.9-x.md`, the probe is green on base and red under that mutation.
```

**9. `lint: 4. заглушка списка с подсказкой внутри скобок` (:167) adds no detection.** verified — mutation probe. M7 (lib/lint.js:21 TODO_VALUE -> `^\[TODO\]`) and M9 (lib/lint.js:386 drop `.replace(/^[-*+]\s+/, '')`) are caught by other tests without it. M8 (remove the `(?!\[TODO)` lookahead at lib/lint.js:24) leaves the suite green: the lookahead is untested and :167 does not guard it, because `TODO_VALUE.test` at lib/lint.js:387 returns first. Commenting the probe line out turns the inline-comment width test red (a >100-char comment line); delete the line instead.

**10. `lint: 4. заглушка «Области» от new красит разобранную задачу и молчит в triage/` (:612): :617 and :620 repeat the green baseline.** verified — mutation probe. `seedGreen` puts `docs/backlog/triage/BS-2.1-d.md` without «Область» (:26), so every green control asserts triage silence; M3 reds the baseline :57 and 24 other tests. :616/:619 are the err-path guard of lib/lint.js:404-405 (M13 reds only this test and :300) and stay.

**11. `lint: чужой проект получает ошибок парности не больше, чем каталогов templates/` (:594): :600-602 repeat :57-63.** verified — mutation probe. M10 (remove the sameDir guard at lib/lint.js:122) reds :57 and :594; M10b (guard -> `if (!existsSync(templates)) return;`) reds only :594, at :603-606. The name promises "no more than" but both halves assert zero.

**12. `lint: 10. quote:before сохраняет снимок до правки, но не скрывает ошибки блока` (:661) spawns the CLI twice where `problems()` suffices.** verified — mutation probe. CLI `lint` (lib/lint.js:669-680) returns 1 iff `errors` is non-empty; :671-673 assert the same fact twice; :675-677 add nothing `problems()` cannot check. M11 (lib/lint.js:611 `!block.before && !quoted` -> `!quoted`) reds only this test.

**13. `lint: живой пин, расходящийся с cli, — ошибка с файлом и строкой` (:858-900) bundles six scenarios.** verified — read. (a) package.json/CI yml red then green :864-872; (b) archive README is live :874-875; (c) CHANGELOG/ADR/archive task.md are history :877-882; (d) backlog card quoting a pin :884-886; (e) npm-form pin :888-891; (f) unpinned cli silent :893-896. A failing assert hides the rest. (f) currently relies on the stale `docs/ROADMAP.md` pin left by (e) at :890; alone it would pass vacuously.

**Must stay:** :57 (green baseline), :71 (EN), :279, :300, :334, :366 (only check of the `lint: ошибок N` summary), :392, :400 (tool-copy negative control), :433 (as a row), :482 and :496 (not merged: never mutation-probed), :513, :543, :564, :628, :691, :720, :764, :783, :840.

## Work to do

- Delete the test at :844-856.
- Delete the probe call at :220 (whole-template result.md, ru/en); keep :218-219 and the per-paragraph loop :221-223.
- Delete the probe at :130 (`2. номер занят дважды`).
- Example with fixture ids (see block):

  ```text
  Fix the probe at :144: create the directory `docs/archive/BS-4-e/minor/BS-4.9-x.md` and expect `/archive\/BS-4-e\/minor\/BS-4\.9-x\.md: в minor\/ пачки только файлы записей/`.
  ```
- Delete the probe line :167 (delete, do not comment out).
- In :612 drop :617 (`new triaged`) and :620; rename the test to `lint: 4. заглушка «Области» от new красит разобранную задачу`.
- In :594 drop :600-602; rename the test to `lint: чужой templates/ не будит гейт парности`.
- In :661 replace both `cli()` calls with `problems()`: `assert.deepEqual(problems(root), [])`, then `assert.ok(problems(root).some((p) => /quoting\.md: цитата ведёт на несуществующий файл reference\/missing\.md/.test(p)))`.
- Split :858 into three tests sharing a small `setConfig` helper: live files red then green after the fix (a, b, e); history and quoted-evidence files are not drift (c, d); unpinned cli is silent (f), which first puts its own stale pin (e.g. `docs/notes.md` with `npx backslop@0.3.0`; `docs/ROADMAP.md` leaves the layout later in the queue). Drop :865 (`warnings(root)` is :334's concern).

## Out of scope

- The `greenProbe`/`toolProbe` helpers and the git-shim merge — BS-130 (`lint-test-helpers-and-merges`).
- Deciding whether the `(?!\[TODO)` lookahead at lib/lint.js:24 is dead code (removal probe measured: suite stays green) — no card in this queue owns it; file a finding if the removal is wanted.
- Merging :482 and :496 into one tool copy: never mutation-probed, saves ~240 ms only.
- Any change under lib/.

## Verification

- `node --test --test-timeout=60000 test/lint.test.mjs; echo rc=$?` -> rc 0, tests 124 (127 after BS-130 (`lint-test-helpers-and-merges`): -1 :844, -2 :220 ru/en, -1 :130, -1 :167, +2 from splitting :858).
- Mutation probe lib/lint.js:438 drop `!m.isFile() ||` -> `lint: 5. каталог в minor/ пачки` red.
- Mutation probe lib/lint.js:122 -> `if (!existsSync(templates)) return;` -> the renamed :594 test red; lib/lint.js:611 `!quoted` -> the :661 test red; lib/lint.js:261 `if (false) err(t.file` -> :392 and :840 red; lib/tasks.js:197 `return true;` -> :691 red.
- `npm test; echo rc=$?` -> rc 0.
