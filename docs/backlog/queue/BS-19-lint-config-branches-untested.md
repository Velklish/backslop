# BS-19 · Пять ветвей `lint` и четыре проверки `loadConfig` не покрыты красной пробой — их можно вырезать, и `npm test` останется зелёным

- **Порядок:** 180
- **Область:** `lib/lint.js`, `lib/config.js`, `test/lint.test.mjs`, `test/config.test.mjs`, [reference/03](../../reference/03-lint.md), [reference/01](../../reference/01-layout.md)
- **Создана:** 2026-09-06
- **Зависимости:** нет

## Контекст

AGENTS.md:7 — «Гейты: `npm test` и `node bin/backslop.js lint` зелёные. Проверку гейта `lint` подтверждай красной пробой в `test/lint.test.mjs`.» — docs/reference/03-lint.md:3 повторяет то же как факт: «у каждого в test/lint.test.mjs есть красная проба — мутация зелёного проекта, без которой гейт нечем отличить от холостого». Оба утверждения проверены сейчас, не выполняются для девяти веток.

lib/lint.js: пять err()-вызовов без пробы — `120` («каталог внутри каталога статуса», в lintTasks), `129` («каталога бэклога нет», lintBacklogLayout), `183` («в архиве только каталоги задач и README.md», lintArchive), `192` («нет task.md — постановки», lintArchive), `248` («нет индекса документации, а ADR есть», lintAdrIndex). test/lint.test.mjs сегодня заканчивается на строке 130 проверками ADR (гейт 8) — ни один из пяти текстов выше среди исходных регексов не встречается.

lib/config.js: четыре проверки loadConfig без пробы — `100` (prefix по PREFIX_RE), `103` (docs — не абсолютный, без `..`, непустая строка), `106` (cli — непустая строка), `107` (gates — массив строк). test/config.test.mjs покрывает только «верхний уровень — объект» (строки 25-33) и «lang/tools» (35-44); prefix/docs/cli/gates — нет.

Мутационная проба выполнена самостоятельно на HEAD `ef087a2` в копии репозитория (scratch): отключение всех пяти веток lib/lint.js одновременно (`if (e.isDirectory())` → блок-заглушка без err — иначе dangling-else ломает соседний `else if`; остальные четыре — `false && err(...)`) — `node --test` даёт `tests 134, pass 134, fail 0`. Отдельным прогоном — то же для четырёх веток loadConfig (`if (false && …)`) — снова `134/134`, `exit 0`. Базовый прогон без мутаций — тоже 134 pass, то есть ни один из этих девяти путей ни разу не покраснел.

## Что сделать

- test/lint.test.mjs: пять probe(...) по образцу существующих (строки ~118-130) — каталог внутри `queue/`; удалённый `docs/backlog/`; посторонний файл в `docs/archive/` (не `README.md`); каталог архива без `task.md`; ADR при удалённом `docs/README.md`.
- test/config.test.mjs: четыре assert.throws по образцу существующих тестов loadConfig (строки 25-44) — некорректный `prefix`; `docs` пустой, абсолютный и с `..` (три случая одной проверки — не обязательно отдельными assert'ами); пустой `cli`; `gates` не массив и массив с не-строкой.
- Каждую новую пробу подтвердить мутацией по шагу 4 процедуры (backslop-репозиторий: коммит теста, потом порча соответствующей ветки, красный — вернуть).
- CHANGELOG: запись о расширении набора (правило AGENTS.md:7 и текст 03-lint.md:3 становятся фактом, а не декларацией).

## Не входит

- Логика lib/lint.js и lib/config.js — правка только теста, не поведения гейтов.
- Гейт или тулинг, автоматически находящий непокрытые ветки (mutation-testing framework) — проект ведёт пробы вручную по конвенции, эта задача просто закрывает конкретный пробел.

## Проверки

- `node --test` (или `npm test`) зелёный, число тестов выросло с 134 минимум до 143.
- На каждую из девяти новых проб — ручная мутация named-ветки: тест красный; после возврата кода — снова зелёный.
- `node bin/backslop.js lint` на самом репозитории backslop зелёный.
