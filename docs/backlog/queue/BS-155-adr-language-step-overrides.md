# BS-155 · English ADRs for the layout language and for step overrides replace ADR-005 and ADR-020

- **Order:** 730
- **Scope:** [01. Layout](../../reference/01-layout.md) § backslop.json
- **Created:** 2026-09-25
- **Dependencies:** BS-103, BS-107, BS-145, BS-152, BS-154

## Context

Two Russian ADRs have no cluster partner and are rewritten here as two English ADRs under new numbers: `docs/adr/adr-005-localization.md` -> language; `docs/adr/adr-020-agents-step-overrides.md` -> step overrides. Both mechanisms are live (evidence below). The BS-145 (`probe-command-adr`) card (earlier) documented that an override of step 4 drops the probe sentence, which the step-override ADR mentions, and the BS-152 (`adr-pin-upgrade-migrate`) card wrote the migrate rule the language ADR links. Line numbers are at 6f6318e.

**How to write a consolidated ADR (applies to every ADR this card creates).** Create it with `node bin/backslop.js adr <slug> --title "<title>"` (the repository runs with `lang: en`, so the English template with Context / Options / Decision / Consequences is used and the number is the next free one; if another card takes the same number first, renumber on rebase: lint gate 8 refuses duplicate numbers). Header: `Status: Accepted`, `Date:` the day you write it, `Deciders: Velklish`. The text must not mention the numbers or file names of the ADRs it replaces, task or finding numbers, run ids, commit hashes, dated measurements or "owner decision of <date>" notes. Cite code by file and function name, not by line number (line numbers below are at base commit 6f6318e and only help you find the code). Options keeps only the rejected alternatives that still explain the choice, each with its cost in one sentence. Then delete the replaced ADR files and replace their rows in `docs/README.md` with one row for the new ADR (topic in English, Status equal to the Status line of the file; keep the table's current column layout). Every remaining reference to a deleted file is handled in the same card: links in `CHANGELOG.md` lose their link markup and old ADR tokens (the CHANGELOG rewrite drops them anyway); links and citations in `docs/reference/`, `docs/GLOSSARY.md` and `AGENTS.md` are dropped, not repointed (rules there carry no ADR citations); code and test comments that cite an old ADR get the new ADR id (`ADR-NNN`) and lose quoted section names the new ADR does not have — comments stay at most two lines and 100 code points per line (`npm test` enforces it).

**1. Language ADR (adr-005).** verified — read, one run.
- Live: `lang` is `ru` or `en` (lib/config.js:15), a config without it is refused (the BS-107 (`marker-only-adapter-ownership`) card removed the `ru` default of :111), `init --lang` sets it; Russian templates in `templates/**`, English in `templates/en/**` with the same paths (lib/templates.js:9-11); every project file is rendered in the config language; CLI messages go through `tr()`; outside a project help and the unknown-command error are bilingual (bin/backslop.js:122-144); `status --json` keys are language-neutral (lib/status.js:34); header fields and headings are read under both names and written with the config label (lib/tasks.js:27-62, :413-418).
- adr-005:14/:22 describe the parity check as "file set and placeholders"; the check also compares skill frontmatter (`name` equals the directory, non-empty `description`), the heading-level sequence outside fences, and refuses Cyrillic in the en layer (lib/templates.js:64-105; tests test/templates.test.mjs:82, :92, :99). It runs only in the tool repository (lib/lint.js:120-124). docs/reference/03-lint.md:28 repeats the narrower wording.
- adr-005:18 "a lang change does not translate existing docs": `migrate` redraws the tool-owned rules docs `backlog/README.md` and `archive/README.md` from the config-language template whenever the stamp is older than the tool (lib/migrate.js:43, :52, :82). Repro: ru project, `lang` switched to `en`, stamp set to 0.10.0, `migrate` -> `docs/archive/README.md` title changed from `# Архив закрытых задач` to `# Closed task archive`; with stamp 0.11.0 the same switch leaves the file untouched. The BS-103 (`unverified-lang-switch-redraw`) card (earlier) may drop the stamp condition for a `lang` change; describe the rule as the code has it when you write the ADR.
- adr-005:23 (the tool README is English, Russian text in README.ru.md) is about this repository's docs; README.ru.md is being dropped (the BS-172 (`readme-english-rewrite`) card), so it is not carried. Do not mention the legacy `Area` header alias (lib/tasks.js:30): the BS-108 (`drop-pre-floor-version-gates`) card removes it.

**2. Step-override ADR (adr-020).** verified — read, one run. Keys "1"-"7" (lib/config.js:207), values non-empty after trimming, no line break, no `<` (lib/config.js:212-222); `init` replaces the whole text of each overridden step, keeps numbers, other steps and the blank line before the worker boundary (lib/init.js:221-246); the trimmed value has every one of the 32 CommonMark ASCII punctuation characters backslash-escaped (`MD_PUNCT`, lib/init.js:23, :215-217, :238). A pin inside an override is escaped and hidden from pin checks: `node -e 'import("./lib/config.js").then(({parseCli,pinRe})=>{const f=parseCli("npx github:Velklish/backslop#v0.11.0"); const esc="npx github:Velklish/backslop#v0.8.0".replace(/[!"#$%&\x27()*+,\-./:;<=>?@[\\\]^_`{|}~]/g,"\\$&"); console.log(pinRe(f).test(esc), pinRe(f).test("npx github:Velklish/backslop#v0.8.0"));})'` -> `false true`, rc=0. Artifacts: adr-020:5 (a run mandate dated after the ADR date, "not proofread"), :11 and :24 (journal links), :18, :54 (a v0.8.0 example).

## Work to do

- Language ADR (`node bin/backslop.js adr localization --title "Layout language: the lang field and a second template set"`): the live rules of item 1, all five parity checks, the redraw of the rules pair on a `lang` change with the condition the code has then (item 1; link the consolidated version-pin/upgrade ADR by its new id). Consequences: a mixed ru/en backlog is normal; a Russian template change without its English twin fails lint in the tool repository; strings built in code are not covered by parity and depend on `tr()` and per-language tests; the language of this repository's own docs is a separate decision. If docs/reference/03-lint.md still says the parity check compares only files and placeholders, list the five checks there.
- Step-override ADR (`node bin/backslop.js adr step-overrides --title "A project overrides a numbered step of the AGENTS.md block from backslop.json"`): context (init owns and restores the block; an extension point below it leaves two contradicting sources; a list of forbidden patterns kept finding bypasses, escaping the whole punctuation class is complete), the rules of item 2, and the step-4 interaction with `probe` as the BS-145 (`probe-command-adr`) card documented it: an override of step 4 replaces the probe sentence too, and `init` prints no note. Consequences: markup shows as text; a pin inside an override is invisible to pin checks and `upgrade` (pins belong in `cli` and `gates`); the step range 1-7 is hard-coded in lib/config.js and lib/init.js and nothing ties it to the step count of templates/agents-section.md; if the template ever puts the value at the start of a block the escaping argument must be revisited.
- Delete adr-005 and adr-020 and replace rows `docs/README.md:16` and `:31` (numbers at 6f6318e) with the two new rows.
- Repoint comments to the new step-override id: lib/init.js:22, :214 and test/init.test.mjs:169. Drop the ADR links in docs/reference/01-layout.md:49 and docs/reference/02-cli.md:33. CHANGELOG.md (:59, :99 at 6f6318e): remove the old ADR-005/ADR-020 tokens and link markup, keep the entry text.

## Out of scope

- Removing README.ru.md with lib/lint.js:35, package.json `files` and test/release.test.mjs:243 — the BS-172 (`readme-english-rewrite`) card.
- The process, seed and tracks ADRs (their own cards).

## Verification

- `ls docs/adr | grep -c -E '^adr-0(05|20)-'` -> `0`.
- `git grep -n -E 'ADR-0(05|20)([^0-9]|$)|adr-0(05|20)-' -- . ':!docs/archive' ':!test/tasks.test.mjs'; echo rc=$?` -> no output, `rc=1` (test/tasks.test.mjs carries `(ADR-005)` inside fixture text of another project).
- `node bin/backslop.js lint; echo rc=$?` -> `rc=0` (gate 1 fails on any link to a deleted ADR file, gate 8 on an ADR without a `docs/README.md` link or a duplicate number).
- `npm test; echo rc=$?` -> `rc=0` (base 6f6318e: 450 tests, 450 pass).
