# BS-140 · Comment gate and template tests: drop the debt list, table lexer and parity, one style

- **Order:** 580
- **Scope:** [Reference](../../reference/README.md)
- **Created:** 2026-09-25
- **Dependencies:** BS-128, BS-95, BS-111, BS-134

## Context

Line numbers are at commit 6f6318e (v0.11.0); if a line moved, find the test by the quoted name. A *mutation probe* means: edit the named lib line in the working tree, run the named test file with `node --test --test-timeout=60000 <file>; echo rc=$?`, read the red test names (lines starting with `✖`), then restore with `git checkout -- lib bin scripts templates`. Base run at 6f6318e: full suite 450 tests, 450 pass. Bug and code cards earlier in the queue changed some of the lib lines named in the mutation probes; apply each probe to the same logic where it lives when you start.

Base runs: test/comment-length.test.mjs 16 tests, 16 pass; test/templates.test.mjs 13 tests, 13 pass. The debt list `test/fixtures/comment-sweep-pending.json` is `{}` and the ceiling at comment-length :80 is `assert.ok(PENDING.size <= 0, …)`: the list can only shrink, down to empty (the comment-debt ADR states this as the legal final state).

**1. Assertions implied by a sibling.** verified — read + mutation probe. comment-length :74 (`scanned.length > 0`) follows from :73 (`emptyTrees` is `[]`): TREES is a 4-entry literal (:14) and `scannedCode` (test/comment-scan.mjs:188) marks a tree empty unless a file lies under it. :81-82 (flattened ids <= 0) follow from :80. :151-153 (appeared/swept) can only fill for files listed in PENDING, empty by :80 (they become live again only if the ceiling rises). :249 compares the lengths of two hard-coded 3-line literals. :252-254 re-assert :261-262 (`unnamedBlocks(twice, []).length === 2`, `unnamedBlocks(twice, [id, id]).length === 0`); probe P3 (blockId hashes only the line count) reds only :246 at :250-251. templates.test.mjs:11 `existsSync(templates/en)` is implied by :12: with templates/en moved away, `templateParity()` returns `['templates/en/ is missing']` (lib/templates.js:78, measured). init.test.mjs:458 asserts the same `existsSync`, placed between the CLI call and its `r.code` check.

**2. Four lexer tests are one table.** verified — read. :194, :209, :217, :225 each redefine `const one = (src) => blocksOf(src).map((r) => [r.line, r.length]);` (:195, :210, :218, :226) and contain only `assert.deepEqual(one(src), expected, why)` rows: 8 at :196-206, 2 at :211-214, 3 at :219-222, 5 at :229-233 = 18. The exception is :199 (`blocksOf('/*\n a\n b\n */\n')[0].lines` trimmed), which stays its own assertion. The comments at :197-198 and :227-228 travel with their rows.

**3. Four templateParity fixture tests are one table.** verified — read. :82, :92, :99 are each `assert.deepEqual(parity({...}), [...])`; :15 hand-builds the same fixture that the `parity()` helper (:33-39) builds. :10 (the real tree) stays apart.

**4. (Part of the debt list.) `каждая запись списка называет файл, который видит обход, и каждый записанный блок ещё существует` (comment-length :182).** unverified — run the check first. With the fixture `{}`, the loop at :185 never runs and `unknownFiles(scanned, new Map())` is `[]`. An unconfirmed probe P1 (fixture `{"lib/version.js":["deadbeef0000"]}`) -> rc 1 with :77, :149 and :182 red — every fixture change it could catch turns :77 red first. The helpers it would exercise are unit-tested at :127 (:144-146, unknownFiles) and :257 (:264-266, staleIds).

**Must stay:** comment-length :73, :117 (:120 `judged === scanned.length` is its only guard), :150, :156, :160, :171, :236 (:80, :127, :246 and :257 go with the debt list); templates :10 (:12), :41, :73, :108, :112, :125, :134, :141, :153. templates :10 and :108 are also caught by the lint self-run (lint.test.mjs:400) but fail in 5-8 ms with a direct message instead of ~230 ms under a gate-11 name: keep both.

**5. The debt list is dead code.** Read at 6f6318e; the removal itself is unverified — run the removal probe first. `cat test/fixtures/comment-sweep-pending.json` prints `{}`. The ceilings are literals: `assert.ok(PENDING.size <= 0, …)` (:80) and `assert.ok(ids <= 0, …)` (:82), so any entry turns the suite red; the list cannot grow again without editing the ceiling, which is a change of decision, not a use of the list. Everything that serves only the list: `PENDING` (:18-20), `blockId` (:23-25), `countOf` (:36-40), `unnamedBlocks` (:43-53), `staleIds` (:56-63), `unknownFiles` (:66-68), the listing branch of `surveyTree` (`appeared`, `swept`, :102-110), the counters `seen`/`lines` and their floors (:121, :124 — with no list they equal the offenders check), the tests `список долга умеет только уменьшаться` (:77), `каждая запись списка называет файл, который видит обход, и каждый записанный блок ещё существует` (:182), `блок опознаётся своим текстом, и список должен его по разу на копию` (:246), `список должен блок по разу на копию: подмена долга закрыта` (:257), and the listed halves of the probe tests (:138-146 `owesNothing`/`swept`/`unknownFiles`, :167-168 `listed.wide`). The consolidated comments ADR (BS-150 (`comments-gate-adr`), after this card) records the gate without a debt list; the alternative of keeping the machinery for a future sweep costs five helpers and four tests that can only reject, and a future sweep would need a new decision anyway. The comment-debt ADR at 6f6318e (docs/adr/adr-035-comment-debt-swept-to-zero-probe-debtor-is-fixture.md) keeps the machinery; that ADR is replaced after this card.

**6. test/comment-scan.mjs: exports only the module uses, a limit defined twice, a style found nowhere else.** verified — grep and a removal probe (450/450).
- **test/comment-scan.mjs exports `commentSpans`, `lineFacts` and `commentBlocks`, which only the module uses.** verified — a probe (450/450). The only importer, test/comment-length.test.mjs:9, imports `{ longBlocks, maskedLines, scannedCode, wideLines }`. docs/GLOSSARY.md:43 and the ADR on comment width name these functions in prose only.
- **The comment limit is defined twice.** verified — grep for the dead defaults; the chosen variant was not probed. `longBlocks(text, limit = 2)` (test/comment-scan.mjs:193) and `wideLines(text, limit = 100)` (:200) are only called as `longBlocks(text, LIMIT)` and `wideLines(text, WIDTH)` (test/comment-length.test.mjs:28, :32), with `LIMIT = 2` and `WIDTH = 100` defined at :12-13. The probe that dropped the defaults stayed green, but it fails open: a later call that omits `limit` would compare `> undefined` and report nothing. Keeping the defaults and dropping the test constants is the fail-closed variant.
- **Two test files use a style found nowhere else.** verified — `grep -c '/\*\*' test/comment-scan.mjs` gives 10, and `grep -oE '[A-Za-z_]\+\+' test/comment-scan.mjs | wc -l` gives 19. test/comment-length.test.mjs has 6 JSDoc blocks (:22, :35, :42, :55, :65, :85) and postfix `++` at :60 and :99. lib/, bin/ and scripts/ have no `/**` and use `i += 1` (26 times in lib).

## Work to do

- In comment-length.test.mjs delete :74 and :249 (the other implied asserts of item 1 go with the debt list below).
- Delete templates.test.mjs:11 and init.test.mjs:458.
- Hoist `one` to module level and replace :194, :209, :217, :225 with an 18-row `CASES` table looped as `for (const c of CASES) test(c.why, () => assert.deepEqual(one(c.src), c.exp))`; keep :199 as its own test and carry the comments at :197-198 and :227-228 onto their rows.
- Replace templates.test.mjs :15, :82, :92, :99 with a 4-row table `{ name, files, expected }` looped as ``test(`template parity: ${name}`, () => assert.deepEqual(parity(files), expected))``; move `parity()` above the table; leave :10 alone.
- Unverified dead code — the debt list (items 4 and 5), as a checklist: (1) Removal probe first: in a scratch commit apply the removal below, then `npm test > t 2>&1; echo rc=$?` and `node bin/backslop.js lint > l 2>&1; echo rc=$?` — both must give rc=0, with the comment-length test count equal to the count before minus the deleted tests; if either is red, restore and record why. (2) Refutation check: search AGENTS.md, docs/reference and the comment ADRs for a rule that requires the list to exist (the comment-debt ADR keeps it for a future sweep; the consolidated comments ADR later replaces it without one) — if the owner has kept the list since, stop and record. (3) Keep the removal: test/comment-length.test.mjs: delete `PENDING`, `blockId`, `countOf`, `unnamedBlocks`, `staleIds`, `unknownFiles`, the listing branch of `surveyTree` and its `seen`/`lines` counters, the four tests named in item 5 and the listed halves of the two probe tests; `surveyTree(files)` returns `{ offenders, wide, judged }`; the floor test keeps `judged === scanned.length`; the probe test keeps: fixture not walked, `carried > 0`, offenders count equals `carried`, the first offender names the fixture and a line; retitle it without a count. Keep the width probe (unlisted half), the width boundary test, the offenders and width tests and every lexer test. Delete `test/fixtures/comment-sweep-pending.json`.
- test/comment-scan.mjs:52,127,165: drop `export` from `commentSpans`, `lineFacts` and `commentBlocks`.
- Keep the defaults at test/comment-scan.mjs:193 and :200 as the single definition of the limits. Delete `LIMIT` and `WIDTH` at test/comment-length.test.mjs:12-13 and call `longBlocks(text)` and `wideLines(text)` at :28 and :32. If the failure messages print the limit, read it from one exported constant in comment-scan.mjs instead of duplicating it.
- test/comment-scan.mjs and test/comment-length.test.mjs: turn `/** … */` comments into `//` comments and postfix `x++` into `x += 1`. The comment-length gate still applies: at most two lines per block and 100 characters per line.

## Out of scope

- Writing the consolidated comments ADR and repointing ADR citations in test comments (BS-150 (`comments-gate-adr`)).

## Verification

- `node --test --test-timeout=60000 test/comment-length.test.mjs; echo rc=$?` -> rc 0; the count is 16 - 4 lexer tests + 18 rows + 1 `.lines` test - the four debt-list tests (:77, :182, :246, :257) = 27.
- `node --test --test-timeout=60000 test/templates.test.mjs test/init.test.mjs; echo rc=$?` -> rc 0, templates 13 tests.
- Mutation probes: deleting `judged++;` (:99) -> :117 red; dropping `fact.codeBefore ||` at test/comment-scan.mjs:172 -> the matching lexer row red; swapping the missing/extra loops at lib/templates.js:79-80 -> a parity row red; moving templates/en away -> templates :10 red (move it back).
- Mutation probes, each reverted with `git checkout -- test`: make `longBlocks` in test/comment-scan.mjs return `[]` -> the probe test is red; make `wideLines` return `[]` -> the width probe is red; delete `judged++` in `surveyTree` -> the floor test is red; add a 3-line `//` block to lib/version.js (then `git checkout -- lib`) -> `инлайн-комментарий не длиннее двух строк…` is red and names `lib/version.js:<line> — длина 3`.
- `git grep -n -E 'PENDING|comment-sweep-pending|blockId|staleIds|unknownFiles|unnamedBlocks' -- test lib docs AGENTS.md; echo rc=$?` -> no output, `rc=1`.
- `grep -c '/\*\*' test/comment-scan.mjs test/comment-length.test.mjs` gives 0 for both, and `grep -nE '[A-Za-z_]\+\+' test/comment-scan.mjs test/comment-length.test.mjs` finds only string fixtures.
- `grep -n "export function \(commentSpans\|lineFacts\|commentBlocks\)" test/comment-scan.mjs; echo rc=$?` → rc=1; `grep -n "LIMIT\|WIDTH" test/comment-length.test.mjs` shows no second definition of the limits.
- `npm test; echo rc=$?` -> rc 0.
