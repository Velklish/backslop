// Носитель долга для пробы гейта комментариев (ADR-035): расширение не .js/.mjs, поэтому
// обход его не судит, а проба читает сама. Блоки ниже нарушают правило намеренно.
export const probe = 1;

// Первый длинный блок: три строки
// комментария подряд
// без кода между ними.
export function first() {}

/*
 * Второй — блок со звёздочками,
 * четыре строки.
 */
export function second() {}

// Третий — в две строки, в пределе по длине; вторая его строка шире 100 знаков, и судит
// его только ширина: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
export function third() {}
