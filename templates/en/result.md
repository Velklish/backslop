# {{id}} · Result

**Closed {{date}}.** [TODO: outcome as a word — completed, rejected, or merged into {{prefix}}-N; without it lint fails and fold N refuses: a bare “Closed” is not an outcome — and exactly what was done, in one paragraph.]

**Verification.** [TODO: how this was verified — gates with numbers from the `backslop gates` summary line, mutation probe, live run.]

**Documentation in the same pass.** [TODO: which documentation files were updated; “not required” is also an answer.]
